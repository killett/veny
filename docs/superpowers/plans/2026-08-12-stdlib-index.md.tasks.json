{
  "planPath": "docs/superpowers/plans/2026-08-12-stdlib-index.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: StdlibIndex dataclass and running source",
      "status": "pending",
      "description": "**Goal:** Create `stdlib_index.py` with the frozen dataclass, first-component lookup, and the running-interpreter constructor, plus the test scaffolding the rest of the plan needs.\n\n**Files:**\n- Create: `stdlib_index.py`\n- Create: `tests/__init__.py`\n- Create: `tests/test_stdlib_index.py`\n- Modify: `pyproject.toml` (the `[tool.pytest.ini_options]` block, currently `pythonpath = [\"src\"]`)\n\n**Acceptance Criteria:**\n- [ ] `\"xml.etree.ElementTree\"` is found in an index whose names are `{\"xml\"}`.\n- [ ] `\"osquery\"` is NOT found in an index whose names are `{\"os\"}`.\n- [ ] `\"mypackage.os\"` is NOT found in an index whose names are `{\"os\"}`.\n- [ ] An empty import name is never standard library, even if `\"\"` is in the names set.\n- [ ] `for_running_interpreter()` contains `os` and `asyncio`, does not contain `numpy`, reports the running version, and reports `source == \"running\"`.\n- [ ] `pytest` collects and runs `tests/test_stdlib_index.py` from the repository root.\n\n**Verify:** `pixi run python -m pytest tests/test_stdlib_index.py -v` -> 5 passed\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 1. `stdlib_index.py` MUST NOT import veny or univ_defs. Do NOT run repo-wide lint/mypy: `veny.py`/`univ_defs.py` carry 1171 pre-existing ruff errors and 158 mypy errors that are out of scope.\n\n```json:metadata\n{\"files\": [\"stdlib_index.py\", \"tests/__init__.py\", \"tests/test_stdlib_index.py\", \"pyproject.toml\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_stdlib_index.py -v\", \"acceptanceCriteria\": [\"dotted import resolves by first component\", \"prefix match is not enough\", \"last component is not used\", \"empty import name is never stdlib\", \"for_running_interpreter reports real stdlib contents and source running\", \"pytest collects tests from the repository root\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Probe target interpreter with degradation",
      "status": "pending",
      "blockedBy": [1],
      "description": "**Goal:** Add `for_interpreter()` and `resolve()` so standard-library truth comes from the interpreter that will actually run the user's script, degrading safely when the probe fails.\n\n**Files:**\n- Modify: `stdlib_index.py` (append to the module created in Task 1)\n- Modify: `tests/test_stdlib_index.py` (append tests)\n\n**Acceptance Criteria:**\n- [ ] `for_interpreter(sys.executable)` returns the same names as `for_running_interpreter()`, tagged `source == \"probe\"`.\n- [ ] A missing interpreter path, a probe timeout, unparseable probe output, and a non-zero probe exit each return `source == \"degraded\"` with a usable (non-empty) name set, and raise nothing.\n- [ ] `resolve(sys.executable)` returns `source == \"running\"` and spawns no subprocess.\n- [ ] `resolve()` of a different interpreter probes exactly once and reports that interpreter's names and version, not the runner's.\n- [ ] `resolve(None)` returns the running index.\n\n**Verify:** `pixi run python -m pytest tests/test_stdlib_index.py -v` -> 12 passed\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 2. The probe is one subprocess returning JSON with both version and names; `# noqa: S603` is required on the `subprocess.run` call. `_is_running_interpreter` uses `shutil.which` plus `os.path.realpath` so command names like `python3.12` resolve.\n\n```json:metadata\n{\"files\": [\"stdlib_index.py\", \"tests/test_stdlib_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_stdlib_index.py -v\", \"acceptanceCriteria\": [\"probe of own interpreter matches running index with source probe\", \"missing interpreter degrades\", \"probe timeout degrades\", \"garbage probe output degrades\", \"non-zero probe exit degrades\", \"resolve of own interpreter spawns no subprocess\", \"resolve of another interpreter uses that interpreter's truth\", \"resolve of None uses running interpreter\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Python 2 and system-package constants",
      "status": "pending",
      "blockedBy": [2],
      "description": "**Goal:** Give the resolver the two small hand-maintained tables that replace the classification half of `known_bad_imports`, with invariant tests that catch typos in them.\n\n**Files:**\n- Modify: `stdlib_index.py`\n- Modify: `tests/test_stdlib_index.py`\n\n**Acceptance Criteria:**\n- [ ] `PYTHON2_ONLY` holds exactly the 20 Python 2 names listed in the design doc and shares no name with the running interpreter's standard library.\n- [ ] Every key of `NEEDS_SYSTEM_PACKAGE` is a real standard-library name on the running interpreter.\n- [ ] `hints_for()` returns only the entries whose names were actually seen.\n\n**Verify:** `pixi run python -m pytest tests/test_stdlib_index.py -v` -> 15 passed\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 3. The 20 names are: BaseHTTPServer, ConfigParser, Cookie, HTMLParser, Queue, SocketServer, StringIO, Tkinter, UserDict, __builtin__, cPickle, cStringIO, cookielib, htmlentitydefs, httplib, tkFileDialog, tkFont, tkMessageBox, urllib2, urlparse. `NEEDS_SYSTEM_PACKAGE` holds one entry: `{\"tkinter\": \"python3-tk\"}`.\n\n```json:metadata\n{\"files\": [\"stdlib_index.py\", \"tests/test_stdlib_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_stdlib_index.py -v\", \"acceptanceCriteria\": [\"PYTHON2_ONLY is disjoint from Python 3 stdlib names\", \"every NEEDS_SYSTEM_PACKAGE key is a stdlib name\", \"hints_for returns only names that were seen\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Wire resolver in, delete hardcoded list",
      "status": "pending",
      "blockedBy": [3],
      "description": "**Goal:** Replace `Options.standard_modules` with `options.stdlib` at all three call sites and delete the 935-line literal.\n\n**Files:**\n- Modify: `veny.py:23` (import block), `veny.py:125-1064` (delete), `veny.py:2410` (main), `veny.py:4323`, `veny.py:4985`, `veny.py:5047`, `veny.py:5181`, `veny.py:5418`\n\n**Acceptance Criteria:**\n- [ ] `rg \"standard_modules\" veny.py` returns nothing.\n- [ ] `veny.py` is roughly 940 lines shorter.\n- [ ] `import veny` still succeeds and `veny.Options().stdlib` contains `os` and `xml.etree.ElementTree` but not `numpy`.\n- [ ] The equivalence check between the deleted list and the new index produces only explainable differences, recorded in the commit message.\n\n**Verify:** `pixi run python -c \"import veny; o = veny.Options(); print('os' in o.stdlib, 'xml.etree.ElementTree' in o.stdlib, 'numpy' in o.stdlib)\"` -> `True True False`\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 4. Run the equivalence-check script (Task 4 Step 1) BEFORE deleting anything and paste its output into the commit body; a removed name outside the expected buckets (3.12/3.13 removals, CPython test artifacts, conda contamination, `__main__`, `test`) is a real regression - stop and report it. Do NOT touch `is_standard_path` / `_is_std_path_cached` at `veny.py:6253`: those classify filesystem paths, a different concern.\n\n```json:metadata\n{\"files\": [\"veny.py\"], \"verifyCommand\": \"pixi run python -c \\\"import veny; o = veny.Options(); print('os' in o.stdlib, 'xml.etree.ElementTree' in o.stdlib, 'numpy' in o.stdlib)\\\"\", \"acceptanceCriteria\": [\"no occurrences of standard_modules remain in veny.py\", \"veny.py is about 940 lines shorter\", \"veny.Options().stdlib classifies os and xml.etree.ElementTree as stdlib and numpy as not\", \"equivalence check output recorded in the commit message\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: Migrate known_bad_imports",
      "status": "pending",
      "blockedBy": [4],
      "description": "**Goal:** Shrink `known_bad_imports` to the six project-specific names and move the Python 2 names into the resolver, behind a pure helper that can be tested without building a virtual environment.\n\n**Files:**\n- Modify: `veny.py:2287` (the `known_bad_imports` set), `veny.py:5355-5361` (`split_imports`)\n- Create: `tests/test_split_imports.py`\n\n**Acceptance Criteria:**\n- [ ] `known_bad_imports` holds exactly six names: `snakeClass`, `GPUampcor`, `pathfinding_salvo_rework`, `DQN`, `bayesOpt`, `non_existent_module`.\n- [ ] A Python 2 name such as `httplib` is still classified bad, now via `stdlib_index.PYTHON2_ONLY`.\n- [ ] Leading-underscore imports are still classified bad.\n- [ ] An ordinary third-party import such as `numpy` is not classified bad.\n- [ ] `seaborn`, `tkinter`, and `msvcrt` are no longer in `known_bad_imports`.\n\n**Verify:** `pixi run python -m pytest tests/test_split_imports.py -v` -> 4 passed\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 5. The classification line moves into a pure `_compute_bad_imports(all_imports, known_bad, py2_only)` helper because `split_imports` builds a real temporary virtual environment and cannot be unit tested as-is. Also delete the orphaned ChatGPT link comment that followed the old set.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_split_imports.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_split_imports.py -v\", \"acceptanceCriteria\": [\"known_bad_imports holds exactly the six project-specific names\", \"httplib is still classified bad via PYTHON2_ONLY\", \"leading-underscore imports are still classified bad\", \"numpy is not classified bad\", \"seaborn, tkinter and msvcrt are no longer blocked\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: Report system-package hints once",
      "status": "pending",
      "blockedBy": [5],
      "description": "**Goal:** Preserve the warning that `tkinter` used to get from `known_bad_imports`, without warning per import occurrence.\n\n**Files:**\n- Modify: `veny.py:2504-2507` (the reporting block in `main`)\n- Modify: `tests/test_split_imports.py`\n\n**Acceptance Criteria:**\n- [ ] When `tkinter` was skipped as standard library, exactly one warning names both `tkinter` and `python3-tk`.\n- [ ] When no hint-carrying module was seen, no such warning is logged.\n\n**Verify:** `pixi run python -m pytest tests/test_split_imports.py -v` -> 6 passed\n\nFull step-by-step code is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 6. The new `warn_about_system_packages(options)` goes immediately above `def split_imports`, and is called from `main` right after the existing `Bad imports:` warning. It reads `options.seen_stdlib_imports`, which Task 4 populates at the three skip sites.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_split_imports.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_split_imports.py -v\", \"acceptanceCriteria\": [\"exactly one warning naming tkinter and python3-tk when tkinter was seen\", \"no warning when no hint-carrying module was seen\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: End-to-end smoke and notebook update",
      "status": "pending",
      "blockedBy": [6],
      "description": "**Goal:** Confirm the whole pipeline classifies a real script correctly, and leave `PROGRESS.md` accurate for the next session.\n\n**Files:**\n- Create: a throwaway script under the scratchpad directory (not committed)\n- Modify: `PROGRESS.md`\n\n**Acceptance Criteria:**\n- [ ] On a script importing `os`, `xml.etree.ElementTree`, `httplib`, and `numpy`, veny skips the first two as standard library, reports `httplib` as a bad import, and lists `numpy` as an uninstalled import to install.\n- [ ] The debug log shows the standard library index line with a name count and a source of `running`, `probe`, or `degraded`.\n- [ ] `PROGRESS.md` names the completed work and the next action.\n\n**Verify:** `pixi run python veny.py --debug --justprint <scratchpad>/smoke_imports.py 2>&1 | rg \"Standard library index|Skipping standard library import|Bad imports|Uninstalled imports\"`\n\nFull step-by-step content is in `docs/superpowers/plans/2026-08-12-stdlib-index.md` -> Task 7. If the machine has no network access, `numpy` may be absent from the uninstalled set; that is acceptable, and the limitation should be noted in the PROGRESS.md gotchas. The index, skip, and bad-import lines are the ones this task actually verifies.\n\n```json:metadata\n{\"files\": [\"PROGRESS.md\"], \"verifyCommand\": \"pixi run python -m pytest tests/ -v\", \"acceptanceCriteria\": [\"os and xml.etree.ElementTree are skipped as standard library\", \"httplib is reported as a bad import\", \"debug log shows the standard library index line with count and source\", \"PROGRESS.md next-action line and gotchas updated\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-12"
}
