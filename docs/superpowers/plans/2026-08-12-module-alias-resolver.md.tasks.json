{
  "planPath": "docs/superpowers/plans/2026-08-12-module-alias-resolver.md",
  "tasks": [
    {
      "id": 1,
      "nativeTaskId": "10",
      "subject": "Task 1: Data model and ranking",
      "status": "completed",
      "description": "**Goal:** Define `Source`, `Candidate`, `Resolution`, the deterministic `rank()` function, and the curated `SEED` mapping.\n\n**Files:**\n- Create: `alias_index.py`\n- Create: `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [ ] `Source` is an `IntEnum` with exactly `OVERRIDE=0`, `CACHE=1`, `INSTALLED=2`, `SEED=3`, `PYPI_CONFIRMED=4`, and no heuristic member\n- [ ] `rank()` sorts by `(source, pip_name)` and is stable across calls\n- [ ] `rank()` deduplicates by `pip_name`, keeping the strongest-evidence occurrence\n- [ ] `SEED` is a curated set of exceptions that are both correct and reachable — derived from the hand-added block at `veny.py:136-146` but not a verbatim copy of it\n- [ ] `alias_index.py` imports nothing from `veny` or `univ_defs`\n\n**Verify:** `pixi run python -m pytest tests/test_alias_index.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 1\".\n\n```json:metadata\n{\"files\": [\"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_alias_index.py -v\", \"acceptanceCriteria\": [\"Source is an IntEnum with OVERRIDE=0, CACHE=1, INSTALLED=2, SEED=3, PYPI_CONFIRMED=4 and no heuristic member\", \"rank() sorts by (source, pip_name) deterministically\", \"rank() dedupes by pip_name keeping strongest evidence\", \"SEED is a curated set of correct, reachable exceptions derived from veny.py:136-146, not a verbatim copy\", \"alias_index.py imports nothing from veny or univ_defs\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "nativeTaskId": "11",
      "subject": "Task 2: Override and cache stores",
      "status": "completed",
      "blockedBy": [1],
      "description": "**Goal:** Load the human override file (hard failure on malformed TOML), and load/save the verified cache (quarantine on corruption, interpreter-tagged entries, persisted import-failure rejections).\n\n**Files:**\n- Modify: `alias_index.py`\n- Modify: `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [ ] `load_overrides()` returns `{}` for a missing file and raises `AliasOverrideError` for malformed TOML\n- [ ] `AliasCache.load()` on a corrupt file renames it to `<name>.corrupt-<timestamp>` and starts empty rather than raising\n- [ ] A cache entry recorded under one interpreter tag is ignored under a different tag\n- [ ] `confirm()` writes through to disk immediately and round-trips\n- [ ] `reject()` persists only `import_failed` rejections; `install_failed` is not persisted\n- [ ] `rejected_names()` reports persisted rejections so the chain can filter them\n\n**Verify:** `pixi run python -m pytest tests/test_alias_index.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 2\".\n\n```json:metadata\n{\"files\": [\"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_alias_index.py -v\", \"acceptanceCriteria\": [\"load_overrides returns {} for a missing file and raises AliasOverrideError on malformed TOML\", \"Any unusable cache -- invalid JSON or valid JSON of the wrong shape -- is quarantined to <name>.corrupt-<timestamp> and resolution proceeds without raising\", \"A cache entry under a different interpreter tag is ignored\", \"confirm() round-trips through disk\", \"reject() persists import_failed but not install_failed\", \"rejected_names() reports persisted rejections\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "nativeTaskId": "12",
      "subject": "Task 3: Target-interpreter probe",
      "status": "completed",
      "blockedBy": [2],
      "description": "**Goal:** Ask the target interpreter for its version and its `packages_distributions()` mapping in one subprocess, degrading to empty on any failure.\n\n**Files:**\n- Modify: `alias_index.py`\n- Modify: `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [ ] `probe_interpreter()` returns `(tag, {top_level: [dist, ...]})` on success\n- [ ] Any failure -- missing executable, non-zero exit, timeout, unparseable output -- logs a warning and returns the running interpreter's tag with an empty mapping\n- [ ] The probe runs exactly one subprocess\n- [ ] Probing the real `sys.executable` returns a tag matching the running interpreter\n\n**Verify:** `pixi run python -m pytest tests/test_alias_index.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 3\".\n\n```json:metadata\n{\"files\": [\"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_alias_index.py -v\", \"acceptanceCriteria\": [\"probe_interpreter returns (tag, packages) on success\", \"Every failure mode degrades to (running tag, {}) with a warning\", \"Exactly one subprocess is spawned\", \"Probing sys.executable reports the running interpreter's version\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "nativeTaskId": "13",
      "subject": "Task 4: PyPI client and wheel inspection",
      "status": "completed",
      "blockedBy": [3],
      "description": "**Goal:** Confirm that a PyPI project declares a given top-level import name, by reading the wheel's zip central directory over an HTTP Range request instead of downloading the wheel.\n\n**Files:**\n- Modify: `alias_index.py`\n- Modify: `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [ ] `PyPIClient.top_levels(name)` returns the declared top-level names, or `None` when the project does not exist or cannot be inspected\n- [ ] The smallest available wheel is selected\n- [ ] A suffix Range request is used; when the server honours it, the whole wheel is never transferred\n- [ ] A server that ignores Range is accepted only below `MAX_WHEEL_BYTES` (5 MB); otherwise the candidate is abandoned (returns `None`)\n- [ ] `.dist-info`, `.data`, and `__pycache__` members are excluded; top-level single-file modules contribute their name without the `.py` suffix\n- [ ] Network errors return `None` and log at debug level, never raise\n- [ ] A wheel whose central directory is not in the first window triggers exactly one widened re-read\n\n**Verify:** `pixi run python -m pytest tests/test_alias_index.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 4\". No test may reach the real network -- every test injects `_FakeFetcher`.\n\n```json:metadata\n{\"files\": [\"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_alias_index.py -v\", \"acceptanceCriteria\": [\"top_levels returns declared names or None\", \"The smallest wheel is chosen\", \"A suffix Range request avoids transferring the wheel body\", \"A Range-ignoring server is accepted only below MAX_WHEEL_BYTES, else the candidate is abandoned\", \"dist-info/.data/__pycache__ excluded; single-file modules contribute their stem\", \"Network errors return None and never raise\", \"A central directory outside the first window triggers one widened re-read\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 5,
      "nativeTaskId": "14",
      "subject": "Task 5: The resolution chain",
      "status": "completed",
      "blockedBy": [4],
      "description": "**Goal:** Assemble `AliasIndex` -- the five-tier walk, name-mutation generation gated behind wheel confirmation, `confirm()`/`reject()` pass-through, and the `build()` / `empty()` constructors.\n\n**Files:**\n- Modify: `alias_index.py`\n- Modify: `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [ ] An override hit returns a single candidate and performs no network access\n- [ ] A cache hit returns a single candidate and performs no network access\n- [ ] `INSTALLED`, `SEED`, and `PYPI_CONFIRMED` tiers all contribute without stopping the walk\n- [ ] A mutation whose wheel does not declare the import name never appears in `candidates`\n- [ ] Persisted rejections are filtered out of `candidates`\n- [ ] With no PyPI client (offline), resolution still returns override/cache/installed/seed candidates\n- [ ] `build()` wires overrides, cache, probe, and client together from a directory and an interpreter\n- [ ] `empty()` builds an index with no probe and no network, for use before an interpreter is known\n\n**Verify:** `pixi run python -m pytest tests/test_alias_index.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 5\". The mutation gate is the load-bearing guarantee: there must be no path from a generated string to pip.\n\n```json:metadata\n{\"files\": [\"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_alias_index.py -v\", \"acceptanceCriteria\": [\"Override hit short-circuits with no network access\", \"Cache hit short-circuits with no network access\", \"INSTALLED, SEED and PYPI_CONFIRMED all contribute without stopping the walk\", \"An unconfirmed mutation never becomes a candidate\", \"Persisted rejections are filtered from candidates\", \"Offline resolution still returns local-evidence candidates\", \"build() wires overrides, cache, probe and client together\", \"empty() builds an index with no probe and no network\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 5.5,
      "nativeTaskId": "18",
      "subject": "Task 5b: Split PyPI client into pypi_client.py",
      "status": "completed",
      "blockedBy": [5],
      "description": "**Goal:** Move the PyPI client out of `alias_index.py` into `pypi_client.py` — a pure move, no behaviour changes.\n\n**Files:**\n- Create: `pypi_client.py`, `tests/test_pypi_client.py`\n- Modify: `alias_index.py`, `tests/test_alias_index.py`\n\n**Acceptance Criteria:**\n- [x] `alias_index.py` reduced (980 -> 681; the ~600 target was missed and accepted by controller ruling)\n- [x] `pypi_client.py` imports nothing from `alias_index`, `veny`, or `univ_defs`\n- [x] Pure move: byte-identical apart from forced module-boundary references\n- [x] Test count unchanged at 64, split 42/22\n- [x] Both files clean under ruff and mypy\n\n**Verify:** `pixi run python -m pytest tests/ -v` -> 64 passed\n\nAdded mid-execution after review found the file past the plan's split threshold.\n\n```json:metadata\n{\"files\": [\"pypi_client.py\", \"tests/test_pypi_client.py\", \"alias_index.py\", \"tests/test_alias_index.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/ -v\", \"acceptanceCriteria\": [\"alias_index.py reduced\", \"pypi_client.py imports nothing from alias_index/veny/univ_defs\", \"Pure move with no behaviour changes\", \"Test count unchanged at 64\", \"Both files clean under ruff and mypy\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 6,
      "nativeTaskId": "15",
      "subject": "Task 6: The attempt loop in veny.py",
      "status": "pending",
      "blockedBy": [5],
      "description": "**Goal:** Add `resolve_and_verify` to `veny.py` -- install candidates in rank order, accept the first that installs *and* imports, uninstall rejects, record outcomes. It installs; the resolver never does.\n\n**Files:**\n- Modify: `veny.py` (add near `check_packages_in_venv`, around line 4372)\n- Modify: `tests/test_split_imports.py`\n\n**Acceptance Criteria:**\n- [ ] Returns the first candidate that installs and imports, and calls `index.confirm()` for it\n- [ ] On import failure, uninstalls the candidate and records an `import_failed` rejection\n- [ ] On install failure, records an `install_failed` rejection and does not uninstall\n- [ ] Stops after `max_attempts` candidates (default 3) even when more remain\n- [ ] Returns `None` for an empty candidate list without calling the installer\n- [ ] Installer, importer, and uninstaller are injected parameters, so the function is testable with no venv\n\n**Verify:** `pixi run python -m pytest tests/test_split_imports.py -v` -> all pass\n\nFull step-by-step code is in the plan document under \"Task 6\".\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_split_imports.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_split_imports.py -v\", \"acceptanceCriteria\": [\"The first candidate that installs and imports is returned and confirmed\", \"An import failure uninstalls the candidate and records import_failed\", \"An install failure records install_failed and does not uninstall\", \"Attempts stop at max_attempts (default 3)\", \"An empty resolution never calls the installer\", \"installer/importer/uninstaller are injected parameters\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 7,
      "nativeTaskId": "16",
      "subject": "Task 7: Rewire call sites, delete the table",
      "status": "pending",
      "blockedBy": [6],
      "description": "**Goal:** Replace `Options.module_aliases` and `Options.reversed_module_aliases` with `Options.aliases`, thread resolved records through `split_imports` and `check_packages_in_venv`, and add the `to_jsonable` handler.\n\n**Files:**\n- Modify: `veny.py:133-1351` (delete `module_aliases`), `veny.py:1352` (delete `reversed_module_aliases`), `veny.py:4372` (`check_packages_in_venv`), `veny.py:4462` (`split_imports`), `main()` near `veny.py:1478`\n- Modify: `univ_defs.py` (`to_jsonable`)\n- Modify: `tests/test_split_imports.py`\n\n**Acceptance Criteria:**\n- [ ] `Options.module_aliases` and `Options.reversed_module_aliases` no longer exist anywhere in the repo\n- [ ] `Options.aliases` is built in `main()` after `options.python_command` resolves\n- [ ] `installed_imports` and `uninstalled_imports` hold `ResolvedImport` records carrying both names, not bare strings\n- [ ] `check_packages_in_venv` reads `record.import_name` and no longer inverts any dict\n- [ ] `univ_defs.to_jsonable` serializes an `AliasIndex` as structured data, not `repr()`\n- [ ] `veny.py` drops roughly 1,210 lines\n- [ ] All existing tests still pass\n\n**Verify:** `pixi run python -m pytest tests/ -v` -> all pass, and `rg -n \"module_aliases\" veny.py` -> no matches\n\nFull step-by-step code is in the plan document under \"Task 7\". Every reader of `installed_imports` / `uninstalled_imports` must be found with `rg`, not from memory -- the plan lists the known line numbers but the search is authoritative.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"univ_defs.py\", \"tests/test_split_imports.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/ -v\", \"acceptanceCriteria\": [\"module_aliases and reversed_module_aliases no longer exist in the repo\", \"Options.aliases is built in main() after python_command resolves\", \"installed_imports and uninstalled_imports hold ResolvedImport records\", \"check_packages_in_venv reads record.import_name and inverts no dict\", \"to_jsonable serializes AliasIndex as structured data, not repr()\", \"veny.py drops roughly 1210 lines\", \"All existing tests still pass\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 8,
      "nativeTaskId": "17",
      "subject": "Task 8: Update PROGRESS.md and close out",
      "status": "pending",
      "blockedBy": [7],
      "description": "**Goal:** Record what the next session needs -- new gotchas, the retired deferred item, the still-open `StdlibIndex` serialization gap -- and confirm the whole suite is green.\n\n**Files:**\n- Modify: `PROGRESS.md`\n\n**Acceptance Criteria:**\n- [ ] **Current work** points at this plan and states the next action\n- [ ] New gotchas recorded: the override file is fatal on malformed TOML; the cache is interpreter-tagged; only import failures persist as rejections; `alias_index.py` must never import `veny`\n- [ ] The deferred `also_needs` item survives; the module-alias half is marked done\n- [ ] The `univ_defs.to_jsonable` deferred item is narrowed to `StdlibIndex` only\n- [ ] Line counts in PROGRESS.md updated to the post-deletion reality\n\n**Verify:** `pixi run python -m pytest tests/ -v` -> all pass, and `git status --short` -> clean\n\nFull gotcha wording is in the plan document under \"Task 8\".\n\n```json:metadata\n{\"files\": [\"PROGRESS.md\"], \"verifyCommand\": \"pixi run python -m pytest tests/ -v\", \"acceptanceCriteria\": [\"Current work points at this plan with a next action\", \"New gotchas recorded (fatal override parse, interpreter-tagged cache, import-only rejections, one-way imports, Range-stripping proxies)\", \"also_needs remains deferred; the module-alias half is marked done\", \"The to_jsonable deferred item is narrowed to StdlibIndex only\", \"Line counts updated to post-deletion reality\"], \"modelTier\": \"mechanical\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-12T00:00:00Z"
}
