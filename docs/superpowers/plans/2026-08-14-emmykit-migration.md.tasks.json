{
  "planPath": "docs/superpowers/plans/2026-08-14-emmykit-migration.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 0: Release emmykit 0.4.0 (external)",
      "status": "pending",
      "description": "**Goal:** emmykit 0.4.0, carrying the JSON type registry and the removal of the embedded script constants, is published and installable.\n\n**Files:**\n- Read: `docs/prompts/2026-08-14-emmykit-json-type-registry.md` (the prompt to paste)\n- No files in this repository change.\n\n**Acceptance Criteria:**\n- [ ] `emmykit` 0.4.0 (or later) is installable from PyPI.\n- [ ] `emmykit.register_json_type` and `emmykit.unregister_json_type` exist on the top-level namespace.\n- [ ] `register_json_type` accepts an encode-only registration (no `tag`, no `decode`) without raising.\n- [ ] The five script constants and `UNIV_DEFS_SYS_PATH_SCRIPT` are gone from emmykit.\n\n**Verify:** `pixi run python -c \"import emmykit as ek; print(ek.__version__); print(callable(ek.register_json_type), hasattr(ek, 'PRINTALL_SCRIPT'))\"` → a version >= 0.4.0, then `True False`\n\nExecuted by the user in the emmykit repository, not this one. See the plan for the three steps.\n\n```json:metadata\n{\"files\": [], \"verifyCommand\": \"pixi run python -c \\\"import emmykit as ek; print(ek.__version__)\\\"\", \"acceptanceCriteria\": [\"emmykit 0.4.0 or later installable from PyPI\", \"register_json_type and unregister_json_type on the top-level namespace\", \"encode-only registration accepted without raising\", \"the five script constants and UNIV_DEFS_SYS_PATH_SCRIPT removed\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 1: Declare emmykit as a dependency",
      "status": "completed",
      "description": "**Goal:** veny declares emmykit in both `pyproject.toml` and `pixi.toml`, and the package is importable from the project environment.\n\n**Files:**\n- Modify: `pyproject.toml:5` (the `dependencies = []` line)\n- Modify: `pixi.toml` (`[pypi-dependencies]`, via `pixi add --pypi`)\n- Modify: `pixi.lock` (regenerated by pixi)\n\n**Acceptance Criteria:**\n- [ ] `pyproject.toml` declares `dependencies = [\"emmykit>=0.3.4\"]`.\n- [ ] `pixi.toml` has `emmykit = \">=0.3.4\"` under `[pypi-dependencies]`.\n- [ ] `pixi run python -c \"import emmykit\"` succeeds.\n- [ ] The existing suite is unchanged: 234 tests pass.\n\n**Verify:** `pixi run python -c \"import emmykit as ek; print(ek.__version__)\" && pixi run test -q 2>&1 | tail -1` → a version string, then `234 passed`\n\nThe `>=0.3.4` floor is deliberately temporary: 0.4.0 does not exist yet and pixi cannot resolve an unreleased version. Task 7 tightens it to `>=0.4.0,<1.0`.\n\n```json:metadata\n{\"files\": [\"pyproject.toml\", \"pixi.toml\", \"pixi.lock\"], \"verifyCommand\": \"pixi run python -c \\\"import emmykit as ek; print(ek.__version__)\\\" && pixi run test -q 2>&1 | tail -1\", \"acceptanceCriteria\": [\"pyproject.toml declares emmykit>=0.3.4\", \"pixi.toml lists emmykit under [pypi-dependencies]\", \"import emmykit succeeds in the pixi env\", \"234 tests still pass\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Stop writing the five helper scripts",
      "status": "completed",
      "description": "**Goal:** veny no longer writes `mydiff.py`, `myaudit.py`, `multireplace.py`, `treeview.py`, `printall.py` or `univ_defs_sys_path_script.py` into `options.my_dir`, and `Options` no longer carries their paths.\n\n**Files:**\n- Modify: `veny.py:94-100` (delete `univ_defs_path` and the six script path fields)\n- Modify: `veny.py:326-331` (delete the six `verify_script` calls)\n- Create: `tests/test_options_surface.py`\n\n**Acceptance Criteria:**\n- [ ] `veny.Options()` has none of `univ_defs_path`, `univ_defs_sys_path_script`, `mydiff_path`, `myaudit_path`, `multireplace_path`, `treeview_path`, `printall_path`.\n- [ ] `veny.Options()` still has `my_dir` and `packages_dir` (the directories main() genuinely uses).\n- [ ] No `verify_script` call remains in `veny.py`.\n- [ ] Full suite green.\n\n**Verify:** `pixi run test -q 2>&1 | tail -1` → `236 passed` (234 baseline + 2 new)\n\nTest code for both tests, and the exact seven lines to delete, are in the plan. Never hand `veny.py` to pre-commit's ruff hooks.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_options_surface.py\"], \"verifyCommand\": \"pixi run test -q 2>&1 | tail -1\", \"acceptanceCriteria\": [\"the seven retired fields are absent from veny.Options()\", \"my_dir and packages_dir still present and correct\", \"no verify_script call remains in veny.py\", \"236 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Register veny's types with emmykit",
      "status": "pending",
      "description": "**Goal:** A new module registers `ResolvedImport`, `StdlibIndex` and `AliasIndex` with emmykit's type registry, giving the first two full round trips and the third an honest encode-only snapshot.\n\n**Files:**\n- Create: `veny_json_types.py`\n- Create: `tests/test_json_types.py`\n\n**Acceptance Criteria:**\n- [ ] `veny_json_types.register_types()` registers all three types and is idempotent.\n- [ ] A `ResolvedImport` survives `to_jsonable` → `json.dumps` → `json.loads` → `from_jsonable` as an equal `ResolvedImport`, including inside a `set`.\n- [ ] A `StdlibIndex` survives the same trip with `names` still a `frozenset`, `python_version` still a tuple, and `source` preserved; an empty index round-trips to an empty frozenset, not `None`.\n- [ ] An `AliasIndex` serializes to a four-key snapshot with no `__type__` and comes back as a plain `dict`.\n- [ ] `veny_json_types.py` imports `emmykit`, `alias_index` and `stdlib_index` only — never `veny`.\n\n**Verify:** `pixi run python -m pytest tests/test_json_types.py -v` → 6 tests pass (running total 242)\n\nBlocked on emmykit 0.4.0. Full module source and all six tests are in the plan.\n\n```json:metadata\n{\"files\": [\"veny_json_types.py\", \"tests/test_json_types.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_json_types.py -v\", \"acceptanceCriteria\": [\"register_types() registers all three types and is idempotent\", \"ResolvedImport round-trips, including inside a set\", \"StdlibIndex round-trips with frozenset names and tuple version; empty index yields an empty frozenset\", \"AliasIndex emits an untagged four-key snapshot and reloads as a plain dict\", \"veny_json_types.py never imports veny\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        0
      ]
    },
    {
      "id": 4,
      "subject": "Task 4: Swap veny.py from univ_defs to emmykit",
      "status": "pending",
      "description": "**Goal:** `veny.py` and the test suite import `emmykit as ek` instead of `univ_defs as ud`, and veny's type registrations are wired in.\n\n**Files:**\n- Modify: `veny.py:28` (import), `veny.py:33-37` (stale comment), `veny.py:45` (comment), and 106 `ud.` call sites\n- Modify: `tests/test_split_imports.py` (8 refs), `tests/test_cache_search.py` (2), `tests/test_alias_index.py` (2)\n- Modify: `tests/test_json_types.py`, `tests/test_options_surface.py` (one new test each)\n\n**Acceptance Criteria:**\n- [ ] `rg '\\bud\\b' veny.py tests/` returns nothing.\n- [ ] `veny.py` imports `emmykit as ek` and calls `veny_json_types.register_types()` at module scope.\n- [ ] Importing `veny` alone is enough to make a `ResolvedImport` round-trip, proved in a subprocess.\n- [ ] Apart from the import block, every changed line in `veny.py` differs only by `ud.` → `ek.`; column alignment untouched.\n- [ ] `veny.Options().args` is still `None`, pinned by a test.\n- [ ] Full suite green.\n\n**Verify:** `pixi run test -q 2>&1 | tail -1` → `244 passed`\n\nThe rename is `sed -i 's/\\bud\\./ek./g'` over four files, then hand-fixing the import blocks. The plan carries the exact replacement text and the diff check that proves nothing else moved.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_split_imports.py\", \"tests/test_cache_search.py\", \"tests/test_alias_index.py\", \"tests/test_json_types.py\", \"tests/test_options_surface.py\"], \"verifyCommand\": \"pixi run test -q 2>&1 | tail -1\", \"acceptanceCriteria\": [\"no ud references remain in veny.py or tests/\", \"veny.py imports emmykit as ek and calls register_types() at module scope\", \"a subprocess importing only veny can round-trip a ResolvedImport\", \"veny.py diff contains only alias-token and import-block changes\", \"veny.Options().args is still None\", \"244 tests pass\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        3
      ]
    },
    {
      "id": 5,
      "subject": "Task 5: Delete univ_defs.py",
      "status": "pending",
      "description": "**Goal:** The 9,757-line local utility file is gone, along with the one test that only made sense while it existed, and every remaining reference to it in code and docs.\n\n**Files:**\n- Delete: `univ_defs.py`\n- Modify: `tests/test_split_imports.py` (delete `test_univ_defs_imports_without_the_alias_modules_beside_it`)\n- Modify: `README.md:52`, `alias_index.py:11`, `alias_index.py:84`, `stdlib_index.py:9`, `venv_cache.py:8`\n\n**Acceptance Criteria:**\n- [ ] `univ_defs.py` no longer exists.\n- [ ] `rg 'univ_defs' -- '*.py' 'README.md'` returns nothing.\n- [ ] README's project structure lists `veny_json_types.py` and records the emmykit dependency.\n- [ ] Full suite green with one test fewer.\n\n**Verify:** `pixi run test -q 2>&1 | tail -1` → `243 passed`\n\nThe deleted test asserted `univ_defs.py` could be copied somewhere on its own and still import — a property of a loose file, meaningless for an installed package. Exact replacement docstring text is in the plan.\n\n```json:metadata\n{\"files\": [\"univ_defs.py\", \"tests/test_split_imports.py\", \"README.md\", \"alias_index.py\", \"stdlib_index.py\", \"venv_cache.py\"], \"verifyCommand\": \"pixi run test -q 2>&1 | tail -1\", \"acceptanceCriteria\": [\"univ_defs.py deleted\", \"no univ_defs references remain in .py files or README.md\", \"README lists veny_json_types.py and the emmykit dependency\", \"243 tests pass\"], \"modelTier\": \"mechanical\"}\n```",
      "blockedBy": [
        4
      ]
    },
    {
      "id": 6,
      "subject": "Task 6: Fail loudly when emmykit is missing",
      "status": "pending",
      "description": "**Goal:** A veny run on an interpreter without emmykit exits with an actionable install message instead of an ImportError traceback.\n\n**Files:**\n- Modify: `veny.py:28` (wrap the import in try/except ImportError)\n- Create: `tests/test_import_guard.py`\n\n**Acceptance Criteria:**\n- [ ] With emmykit unimportable, `import veny` exits non-zero with a message naming the package and the install command.\n- [ ] The message reaches stderr, not stdout.\n- [ ] With emmykit present, `import veny` still succeeds — the guard does not misfire.\n- [ ] The guard catches `ImportError` only, not bare `Exception`.\n\n**Verify:** `pixi run python -m pytest tests/test_import_guard.py -v` → 2 tests pass (running total 245)\n\nBoth tests use a subprocess; `sys.modules['emmykit'] = None` is what makes the import raise without touching the filesystem. Guard source is in the plan.\n\n```json:metadata\n{\"files\": [\"veny.py\", \"tests/test_import_guard.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_import_guard.py -v\", \"acceptanceCriteria\": [\"missing emmykit produces a non-zero exit with the package name and pip install command on stderr\", \"no traceback on stdout\", \"veny still imports normally when emmykit is present\", \"the guard catches ImportError only\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        4
      ]
    },
    {
      "id": 7,
      "subject": "Task 7: Pin 0.4.0, verify live, record migration",
      "status": "pending",
      "description": "**Goal:** **USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\nThe dependency floor is raised to the released 0.4.0, veny is proven to work end-to-end against a real script, and PROGRESS.md records the outcome.\n\n**Files:**\n- Modify: `pyproject.toml:5`, `pixi.toml` (`[pypi-dependencies]`), `pixi.lock`\n- Modify: `PROGRESS.md`\n\n**Acceptance Criteria:**\n- [ ] `pyproject.toml` and `pixi.toml` both pin `emmykit>=0.4.0,<1.0`, and `pixi install` resolves.\n- [ ] Full suite green: 245 tests pass.\n- [ ] `ruff check veny.py --statistics` total no worse than the baseline captured in Step 1.\n- [ ] `mypy veny.py veny_json_types.py` reports no new errors versus that baseline.\n- [ ] A live run of veny against a real Python script creates or reuses a virtual environment and runs the script successfully, with captured stdout showing the script's own output.\n- [ ] After that live run, none of the five helper scripts or the sys-path shim has been re-created or modified in `~/veny` (mtime diff against the pre-run capture).\n- [ ] The saved last-used options JSON from that run contains no `univ_defs` key and no `ResolvedImport(` repr string.\n- [ ] PROGRESS.md's Current work block records the migration as complete and the status of both cross-repo prompts.\n\n**Verify:** `pixi run test -q 2>&1 | tail -1` → `245 passed`, plus the live-run evidence captured in the plan's Steps 3-5\n\n```json:metadata\n{\"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"files\": [\"pyproject.toml\", \"pixi.toml\", \"pixi.lock\", \"PROGRESS.md\"], \"verifyCommand\": \"pixi run test -q 2>&1 | tail -1 && pixi run python veny.py /tmp/veny_live_check.py\", \"acceptanceCriteria\": [\"pyproject.toml and pixi.toml pin emmykit>=0.4.0,<1.0 and pixi install resolves\", \"245 tests pass\", \"ruff check veny.py --statistics no worse than the Step 1 baseline\", \"mypy veny.py veny_json_types.py reports no new errors versus baseline\", \"a live veny run executes a real script successfully and prints its output\", \"no helper script in ~/veny was re-created or modified during the live run\", \"the run's options JSON contains no univ_defs key and no ResolvedImport( repr string\", \"PROGRESS.md records the migration as complete\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        5,
        6
      ]
    }
  ],
  "lastUpdated": "2026-08-14T22:06:56-07:00"
}
