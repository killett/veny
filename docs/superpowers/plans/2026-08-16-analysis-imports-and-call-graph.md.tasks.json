{
  "planPath": "docs/superpowers/plans/2026-08-16-analysis-imports-and-call-graph.md",
  "tasks": [
    {
      "id": 0,
      "subject": "Task 1: Characterize the call graph before moving it",
      "status": "completed",
      "description": "**Goal:** The call-graph half of the pipeline has tests for the first time, describing exactly what it does today.\n\n**Files:**\n- Create: `tests/test_call_graph.py`\n\n**Acceptance Criteria:**\n- [ ] Six tests pass, covering: module/function collection, in-function imports, the `Class.method` naming, the `::` graph-key format, base-class method fallback, and recursion-guarded traversal\n- [ ] Every expected value is obtained by running the code, not predicted\n- [ ] No source file is modified in this task\n- [ ] Deleting `build_call_graph`'s base-class fallback (`cli.py:1419-1433`) makes a test fail\n\n**Verify:** `pixi run python -m pytest tests/test_call_graph.py -v` \u2192 6 passed, then `pixi run test` \u2192 289 passed\n\n`build_call_graph`, `collect_used_imports`, `_analyze_module`, `split_function_name`, `_resolve`, `FunctionInfo` and `ModuleInfo` have ZERO tests. Tests come before the move because this is not a byte-identical move of pure functions the way 3a's literals.py was.\n\nTRAP: `ImportFunctionCollector` fills its own `self.base_classes`; only `_analyze_module` (`cli.py:1575`) copies it onto the `ModuleInfo`. A test constructing the collector directly sees `{}` and wrongly concludes inheritance is broken. Every test goes through `_analyze_module` against a real file on `tmp_path`. Full test source in the plan, Task 1.\n\n```json:metadata\n{\"files\": [\"tests/test_call_graph.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_call_graph.py -v\", \"acceptanceCriteria\": [\"6 tests pass\", \"values measured not predicted\", \"no source file modified\", \"base-class fallback mutation kills a test\", \"289 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 1,
      "subject": "Task 2: Characterize the import collector's paths",
      "status": "completed",
      "blockedBy": [
        0
      ],
      "description": "**Goal:** The 518-line `ImportFunctionCollector` \u2014 the largest and least-tested symbol in this extraction \u2014 has direct tests of the paths a refactor is most likely to break.\n\n**Files:**\n- Create: `tests/test_import_collector.py`\n\n**Acceptance Criteria:**\n- [ ] Five tests pass, covering: dotted-name normalization, `from X import Y` handling, top-level vs in-function import separation, dynamic `importlib.import_module`, and alias recording\n- [ ] Every expected value is obtained by running the code, not predicted\n- [ ] No source file is modified in this task\n- [ ] All five tests go through `_analyze_module`, for the reason given in Task 1\n\n**Verify:** `pixi run python -m pytest tests/test_import_collector.py -v` \u2192 5 passed, then `pixi run test` \u2192 294 passed\n\nUnlike Task 1, the plan does NOT hand you the expected values \u2014 measure each fixture with the probe in the plan's Task 2 before writing the assertion, and paste the observed output into your report. If an observed value contradicts what a test name implies, report before writing the test.\n\n```json:metadata\n{\"files\": [\"tests/test_import_collector.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_import_collector.py -v\", \"acceptanceCriteria\": [\"5 tests pass\", \"values measured with the probe, output pasted in report\", \"no source file modified\", \"all tests go through _analyze_module\", \"294 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 3: Extract analysis/call_graph.py",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** The call-graph types and traversal live in their own module, behaviour unchanged, with Task 1's tests proving it.\n\n**Files:**\n- Create: `src/veny/analysis/call_graph.py`\n- Modify: `src/veny/cli.py` \u2014 delete `FunctionInfo` (830-839), `ModuleInfo` (841-852), `_SEP` (1375), `split_function_name` (1378-1391), `_resolve` (1394-1396), `build_call_graph` (1399-1450), `collect_used_imports` (1453-1513); import from the new module\n- Modify: `tests/test_layering.py` \u2014 add the `FORBIDDEN` entry\n- Modify: `tests/test_call_graph.py` \u2014 update import paths\n\n**Acceptance Criteria:**\n- [ ] `call_graph.py` holds all seven symbols, moved verbatim\n- [ ] `call_graph.py` imports only the standard library \u2014 nothing from `veny` at all\n- [ ] `tests/test_layering.py`'s `FORBIDDEN` gains `\"analysis/call_graph\"`, forbidding every other `veny` module, and the layering tests pass\n- [ ] `tests/test_call_graph.py` imports `build_call_graph` and `collect_used_imports` from `veny.analysis.call_graph`; `_analyze_module` and `Options` still come from `veny.cli`\n- [ ] All tests pass\n\n**Verify:** `pixi run test` \u2192 294 passed\n\nPURE MOVE \u2014 do not fix, tidy, reformat, rename or re-comment anything. `FunctionInfo.ast_node` is Task 6's job, kept separate so this move stays auditable. These seven symbols read no `Options`, which is why this module goes first. The new import in `cli.py` must sit BELOW the emmykit availability guard \u2014 3a hit this and broke the friendly install message.\n\n```json:metadata\n{\"files\": [\"src/veny/analysis/call_graph.py\", \"src/veny/cli.py\", \"tests/test_layering.py\", \"tests/test_call_graph.py\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"seven symbols moved verbatim\", \"no veny imports at all\", \"FORBIDDEN entry added and layering green\", \"test imports updated\", \"294 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 4: Introduce ImportScan, extract analysis/imports.py",
      "status": "completed",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** The AST import collector and the per-import resolver move out, taking a mutable `ImportScan` and an injected `is_stdlib` predicate instead of `Options`.\n\n**Files:**\n- Create: `src/veny/analysis/scan_state.py`, `src/veny/analysis/imports.py`\n- Modify: `src/veny/cli.py` \u2014 delete `SysPathVisitor` (689-728), `process_import` (731-828), `ImportFunctionCollector` (855-1372); import from the new modules\n- Modify: `tests/test_layering.py` \u2014 two more `FORBIDDEN` entries\n- Modify: `tests/test_import_collector.py` \u2014 update import paths if needed\n\n**Acceptance Criteria:**\n- [ ] `ImportScan` is a mutable dataclass carrying exactly the seven fields the scan accumulates, with the same element types `Options` declares (note `subfolders` is `list[str]`, not `list[Path]`)\n- [ ] `process_import(scan, module_name, file_path, *, is_stdlib)` takes no `Options` and no `StdlibIndex`\n- [ ] `ImportFunctionCollector(scan, module_name, file_path)` takes no `Options`\n- [ ] `rg -n 'options\\.' src/veny/analysis/` returns nothing\n- [ ] `analysis/imports.py` imports nothing from `veny.cli`, `veny.stdlib_index`, `veny.alias_index`, `veny.venv_cache`, `veny.pypi_client` or `veny.json_types`\n- [ ] `tests/test_layering.py` covers both new modules\n- [ ] All tests pass\n\n**Verify:** `pixi run test` \u2192 294 passed\n\n`ImportScan` is forced, not chosen: once these symbols live under `analysis/`, naming `Options` at all would make the module import `cli`, which the layering guard fails on. It is mutable (a scan accumulates into it) and lives in its own module because `scan.py` imports `imports.py`, so putting it in either would be circular. Full field table and signatures in the plan, Task 4.\n\n```json:metadata\n{\"files\": [\"src/veny/analysis/scan_state.py\", \"src/veny/analysis/imports.py\", \"src/veny/cli.py\", \"tests/test_layering.py\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"ImportScan carries seven fields with Options' declared types\", \"process_import takes scan and is_stdlib, no Options\", \"collector takes scan, no Options\", \"no options. in analysis/\", \"no stdlib_index import\", \"layering covers both modules\", \"294 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 5: Extract analysis/scan.py",
      "status": "completed",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** The scanner itself moves out, and `cli.py` becomes a caller that builds an `ImportScan` and reads the result.\n\n**Files:**\n- Create: `src/veny/analysis/scan.py`, `tests/test_imports.py`\n- Modify: `src/veny/cli.py` \u2014 delete `_analyze_module` (1516-1577), `_enqueue_top_level_imports` (1580-1608), `find_imports_in_script` (1611-1813); add the wrapper\n- Modify: `tests/test_layering.py` \u2014 one more `FORBIDDEN` entry\n- Modify: `tests/test_literals.py` \u2014 remove the misplaced `SysPathVisitor` test\n- Modify: `tests/test_call_graph.py`, `tests/test_import_collector.py`, `tests/test_split_imports.py` \u2014 update import paths\n\n**Acceptance Criteria:**\n- [ ] `find_imports_in_script(settings, first_path, *, is_stdlib)` returns an `ImportScan` and takes no `Options`\n- [ ] `cli.py` keeps a thin `find_imports_in_script(options, first_path)` wrapper that builds `Settings`, calls the new function, and copies the seven fields onto `options`, with a comment saying it is a bridge\n- [ ] `analysis/scan.py` imports nothing from `veny.cli` or any index module\n- [ ] The `SysPathVisitor` test moves from `tests/test_literals.py` to a new `tests/test_imports.py` and imports from `veny.analysis.imports`\n- [ ] `rg -n 'options\\.' src/veny/analysis/` returns nothing\n- [ ] All tests pass, and the live multi-module run in the plan still discovers imports correctly\n\n**Verify:** `pixi run test` \u2192 294 passed\n\nHIGHEST-RISK STEP: `get_all_imports` calls `find_imports_in_script` once per file and relies on results accumulating across calls, but a wrapper that assigns would overwrite them. Measure the current accumulate-vs-assign behaviour before writing the wrapper and reproduce it exactly; report which you found and what you did. Preserve the stdlib-skip logging asymmetry \u2014 do not tidy the two `continue`-before-`process_import` paths into one.\n\n```json:metadata\n{\"files\": [\"src/veny/analysis/scan.py\", \"src/veny/cli.py\", \"tests/test_layering.py\", \"tests/test_literals.py\", \"tests/test_imports.py\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"find_imports_in_script returns ImportScan, no Options\", \"cli.py wrapper copies back with a bridge comment\", \"get_all_imports accumulation preserved and reported\", \"no cli or index imports in scan.py\", \"SysPathVisitor test relocated\", \"no options. in analysis/\", \"live multi-module run correct\", \"294 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 6: Retire FunctionInfo.ast_node",
      "status": "completed",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** A field that has been write-only since phase 1 is removed, at the moment the design said to decide it.\n\n**Files:**\n- Modify: `src/veny/analysis/call_graph.py` \u2014 `FunctionInfo.__init__`\n- Modify: `src/veny/analysis/imports.py` \u2014 the one construction site\n\n**Acceptance Criteria:**\n- [ ] `FunctionInfo` no longer stores `ast_node`, and its constructor no longer takes `node`\n- [ ] `rg -n 'ast_node' src/ tests/` returns nothing\n- [ ] All tests pass, with no change in what any of them assert\n\n**Verify:** `pixi run test` \u2192 294 passed\n\nPROGRESS.md parked this in phase 1: its only reader was the per-function loop deleted with the visitor block, and phase 3's call-graph split is \"the right moment to decide whether the field has a consumer in the new shape.\" Confirm the `rg` result yourself before deleting \u2014 the parked note's line number is already stale. If more than one hit, or if any test needs editing to keep passing, stop and report: the field was not write-only.\n\n```json:metadata\n{\"files\": [\"src/veny/analysis/call_graph.py\", \"src/veny/analysis/imports.py\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"field and constructor parameter removed\", \"rg ast_node returns nothing\", \"294 tests pass with no test edited\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 7: Run the gates and update PROGRESS",
      "status": "completed",
      "blockedBy": [
        5
      ],
      "description": "**Goal:** Every gate confirmed, and `PROGRESS.md` points at plan 3c.\n\n**Files:**\n- Modify: `PROGRESS.md`\n\n**Acceptance Criteria:**\n- [ ] `pixi run test` passes with 294 tests\n- [ ] `ruff check .` zero; `ruff format --check .` every file formatted\n- [ ] `pixi run typecheck 2>&1 | tail -1` at or below 37\n- [ ] `pixi run smoke` green, or the note says explicitly it was skipped and why\n- [ ] A live `pixi run veny --no-cache` run still builds a venv and runs the script\n- [ ] `PROGRESS.md` **Current work** names plan 3c as the next action, with the measured `wc -l src/veny/cli.py`, and updates the 3a-3e table\n- [ ] Gotchas records the `base_classes` copy trap and the `ImportScan` bridge\n- [ ] Deferred items records the remaining coverage gaps and the three design amendments\n\n**Verify:** `pixi run test && pixi run lint && pixi run smoke` \u2192 all green\n\nThe exact Gotchas and Deferred-items wording is in the plan, Task 7 Step 2. PROGRESS.md is long-form prose, not a bullet dump \u2014 match the surrounding register.\n\n```json:metadata\n{\"files\": [\"PROGRESS.md\"], \"verifyCommand\": \"pixi run test && pixi run lint\", \"acceptanceCriteria\": [\"294 tests pass\", \"ruff clean\", \"mypy <= 37\", \"smoke green or explicitly noted skipped\", \"live run works\", \"PROGRESS names plan 3c with measured line count\", \"base_classes trap and ImportScan bridge recorded\", \"coverage gaps and design amendments recorded\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-17T19:23:50-07:00"
}
