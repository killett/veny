{
  "planPath": "docs/superpowers/plans/2026-08-21-state-model-values.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Non-script argument is a usage error",
      "status": "completed",
      "description": "**Goal:** `veny somedir/` and `veny /no/such/file.py` return exit 2 with a message instead of an uncaught traceback, and the ~55 lines of unreachable folder-scanning code go with them.\n\n**Files:**\n- Modify: `src/veny/pipeline.py:246-358` (`list_packages`, `stayed_out_dir`, `get_all_imports`), `src/veny/pipeline.py:394-421` (`resolve_target`)\n- Modify: `tests/test_import_discovery.py:224-270` (delete the folder-walk test, add the two usage-error tests)\n- Delete rows: `docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming-wiring-index.md` \u2014 every row for `pipeline.py:291`, `:297`, `:317`/`:318` and `:326`-`:346` (16 rows)\n\n**Acceptance Criteria:**\n- [ ] `pipeline.get_all_imports` and `pipeline.stayed_out_dir` no longer exist; `rg -n 'get_all_imports|stayed_out_dir' src/ tests/` returns nothing.\n- [ ] `list_packages` has one arm: a file that `ek.is_python_script` accepts. Everything else is `UsageError`.\n- [ ] `resolve_target` raises `UsageError` \u2014 not `IsADirectoryError`, not `FileNotFoundError` \u2014 when the positional argument is a directory or does not exist.\n- [ ] `veny <directory>` and `veny /no/such/script.py` both return **2** through `cli.main`, and log the message rather than printing a traceback.\n- [ ] The 16 wiring-index rows are struck from the 3e index with a one-line note naming this task, not silently removed.\n- [ ] A test proves the directory case is a status and not a traceback, and a second proves the missing-file case is.\n\n**Verify:** `pixi run test -k \"usage_error or list_packages or resolve_target\" -v` \u2192 all pass; `pixi run test` \u2192 no fewer than 407 passed.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 1. User ruling 2026-08-21: folder scanning is DELETED, not revived. `IsADirectoryError` is a subclass of `OSError`, so its except clause must come first.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"tests/test_import_discovery.py\", \"docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming-wiring-index.md\"], \"verifyCommand\": \"pixi run test -k \\\"usage_error or list_packages or resolve_target\\\" -v && pixi run test\", \"acceptanceCriteria\": [\"get_all_imports and stayed_out_dir no longer exist anywhere under src/ or tests/\", \"list_packages accepts only a file ek.is_python_script accepts; everything else raises UsageError\", \"resolve_target raises UsageError for a directory and for a missing file\", \"cli.main returns 2 for both cases with no traceback\", \"the 16 wiring-index rows are struck with a note naming this task\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Target \u2014 what is being run",
      "status": "completed",
      "description": "**Goal:** `resolve_target` returns a frozen `Target` instead of writing four fields onto `Options`, and every consumer of those fields takes the `Target`.\n\n**Files:**\n- Modify: `src/veny/state.py` (add `Target`)\n- Modify: `src/veny/pipeline.py:394-421` (`resolve_target`), `:424-466` (`feeling_lucky`), `:553-579` (`_load_last_used`), `:705-940` (`run`)\n- Modify: `src/veny/cli.py:170-200` (`main`)\n- Create: `tests/test_state_values.py`\n\n**Acceptance Criteria:**\n- [ ] `state.Target` is `@dataclass(frozen=True)` with exactly `python_script: Path`, `script_dir: Path`, `script_args: tuple[str, ...]`, `python_command: str`, `timestamp: str`.\n- [ ] `resolve_target(args) -> Target | None` takes the parsed namespace, not an `Options`, and returns `None` for a scriptless run.\n- [ ] `python_command` is filled by `dataclasses.replace` inside `run` once `ek.find_preferred_python_version()` has answered; `resolve_target` leaves it `\"\"`.\n- [ ] `Options.python_script`, `Options.script_dir`, `Options.script_args`, `Options.python_command`, `Options.script_name`, `Options.timestamp` are gone from `run_options.py`.\n- [ ] `Target` is immutable in fact: assigning to a field raises `FrozenInstanceError`, and a test proves it.\n- [ ] `feeling_lucky` takes `(args, target, *, options, pathlibcutoff, rawlog)` \u2014 `options` only because `last_used.load_last_used_venv_python` is typed against `ek.Options` until 4b.\n\n**Verify:** `pixi run test tests/test_state_values.py -v` \u2192 all pass; `pixi run test` \u2192 green.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 2. `script_name` is design amendment 3 \u2014 written once to \"\" and never read; delete rather than rehome.\n\n```json:metadata\n{\"files\": [\"src/veny/state.py\", \"src/veny/pipeline.py\", \"src/veny/cli.py\", \"src/veny/run_options.py\", \"tests/test_state_values.py\"], \"verifyCommand\": \"pixi run test tests/test_state_values.py -v && pixi run test\", \"acceptanceCriteria\": [\"state.Target is frozen with exactly the five named fields\", \"resolve_target takes argparse.Namespace and returns Target | None\", \"python_command is filled via dataclasses.replace in run, not mutated\", \"the six drained fields are gone from run_options.py\", \"a test proves Target raises FrozenInstanceError on assignment\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        1
      ]
    },
    {
      "id": 3,
      "subject": "Task 3: Widen Settings and build it once",
      "status": "completed",
      "description": "**Goal:** `Settings` carries all ten run invariants, is constructed exactly once per run, and is handed to both `dict_of_custom_modules` and `analysis_scan.find_imports_in_script` \u2014 closing the four dead-argument rows at `pipeline.py:125` by construction.\n\n**Files:**\n- Modify: `src/veny/settings.py`\n- Modify: `src/veny/pipeline.py:98-143` (`find_imports_in_script`), `:209-244` (`split_imports`), `:468-524` (`blank_slate`), `:705-940` (`run`)\n- Modify: `src/veny/cli.py` (build the `Settings`)\n- Modify: `src/veny/run_options.py` (delete the ten rehomed fields and the three dead ones)\n- Modify: `tests/test_state_values.py`\n\n**Acceptance Criteria:**\n- [ ] `Settings` has exactly ten fields: `my_name`, `my_dir`, `cwd`, `venv_name`, `stay_out_list`, `search_above_this_dir`, `rawlog`, `known_bad_imports`, `also_needs`, `extra_requirements_file`.\n- [ ] Every collection field is immutable at the type level: `stay_out_list: tuple[str, ...]`, `known_bad_imports: frozenset[str]`, `also_needs: Mapping[str, tuple[str, ...]]` backed by `MappingProxyType`.\n- [ ] `rg -c 'Settings\\(' src/veny/` reports exactly **one** construction site, in `cli.py`.\n- [ ] `pipeline.find_imports_in_script` no longer builds a `Settings`; it takes one.\n- [ ] `Options.unusual_imports`, `Options.max_checks` and `Options.check_interval` are deleted (design amendment 2 \u2014 zero readers under `src/`).\n- [ ] `Options.my_name`, `my_dir`, `cwd`, `venv_name`, `stay_out_list`, `search_above_this_dir`, `known_bad_imports`, `also_needs`, `extra_requirements_file` are deleted; `home` and `rawlog` stay (4b's).\n\n**Verify:** `pixi run test -v` \u2192 green; `rg -c 'Settings\\(' src/veny/` \u2192 `src/veny/cli.py:1`.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 3. Carry the existing comments on `also_needs`, `known_bad_imports` and `stay_out_list` over verbatim when they become module constants.\n\n```json:metadata\n{\"files\": [\"src/veny/settings.py\", \"src/veny/cli.py\", \"src/veny/pipeline.py\", \"src/veny/run_options.py\", \"tests/test_state_values.py\"], \"verifyCommand\": \"pixi run test -v && rg -c 'Settings\\\\(' src/veny/\", \"acceptanceCriteria\": [\"Settings has exactly the ten named fields\", \"collection fields are tuple/frozenset/MappingProxyType and reject mutation\", \"exactly one Settings construction site exists, in cli.py\", \"find_imports_in_script takes a Settings rather than building one\", \"unusual_imports, max_checks and check_interval are deleted from Options\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        2
      ]
    },
    {
      "id": 4,
      "subject": "Task 4: ImportScan as the scan's product",
      "status": "completed",
      "description": "**Goal:** `list_packages` builds one `ImportScan`, seeds it with the custom-module dict, hands it to the scanner and returns it. The seven scan fields leave `Options`.\n\n**Files:**\n- Modify: `src/veny/pipeline.py:98-160` (`find_imports_in_script`, `warn_about_system_packages`), `:246-312` (`list_packages`), `:527-551` (`report`), `:705-940` (`run`)\n- Modify: `src/veny/run_options.py` (delete the seven fields plus `total_imports`)\n- Modify: `tests/test_split_imports.py`, `tests/test_import_discovery.py`\n\n**Acceptance Criteria:**\n- [ ] `list_packages(settings, target, scan, *, is_stdlib) -> ImportScan` returns the scan it filled.\n- [ ] The scan is seeded with `custom_modules` before the walk: a test proves that a custom module found by `dict_of_custom_modules` is visible to the scanner.\n- [ ] `warn_about_system_packages(scan)` takes the scan.\n- [ ] `report` takes the scan (the `Requirements` half lands in Task 5).\n- [ ] `Options.all_imports`, `custom_modules`, `loaded_custom_modules`, `samedir_files`, `subfolders`, `sys_path_hints`, `seen_stdlib_imports`, `total_imports` are deleted.\n- [ ] `rg -n 'ImportScan\\(' src/veny/pipeline.py` reports exactly **one** site.\n\n**Verify:** `pixi run test tests/test_split_imports.py tests/test_import_discovery.py -v` \u2192 green.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 4. GOTCHA: `analysis/scan.py` mutates the ImportScan **in place** and never rebinds a field, and `dict_of_custom_modules()` fills `custom_modules` before the scan runs. The scan must be SEEDED, not merely read afterwards. `all_imports` is the one field rebound (by the regex filter) and only after the scanner has finished \u2014 do not move that filter earlier.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"src/veny/run_options.py\", \"tests/test_split_imports.py\", \"tests/test_import_discovery.py\"], \"verifyCommand\": \"pixi run test tests/test_split_imports.py tests/test_import_discovery.py -v && pixi run test\", \"acceptanceCriteria\": [\"list_packages returns the ImportScan it filled\", \"a test proves the scan is seeded with custom_modules before the walk\", \"warn_about_system_packages takes the scan\", \"the seven scan fields and total_imports are deleted from Options\", \"exactly one ImportScan construction site remains in pipeline.py\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        3
      ]
    },
    {
      "id": 5,
      "subject": "Task 5: Requirements as the classification product",
      "status": "completed",
      "description": "**Goal:** `split_imports` returns the `Requirements` `classify` already produces, the copy-back onto `Options` goes, and every downstream reader takes the value.\n\n**Files:**\n- Modify: `src/veny/pipeline.py:209-244` (`split_imports`), `:527-551` (`report`), `:581-702` (`setup_virtualenv`), `:705-940` (`run`)\n- Modify: `src/veny/run_options.py` (delete `bad_imports`, `uninstalled_imports`, `extra_requirements`)\n- Modify: `tests/test_split_imports.py`, `tests/test_classify.py`, `tests/test_manifest_writing.py`\n\n**Acceptance Criteria:**\n- [ ] `pipeline.split_imports(settings, scan, *, args, aliases, extra_requirements, probe) -> state.Requirements` \u2014 no copy-back, no `Options`.\n- [ ] `report(settings, scan, requirements)` reads `requirements.uninstalled`, `requirements.bad`, `scan.samedir_files`, `scan.subfolders`.\n- [ ] `setup_virtualenv` returns the repaired requirements rather than mutating `options.uninstalled_imports` (the `VenvHandle` and `install_succeeded` halves land in Task 6).\n- [ ] `verify.verify_and_repair_imports`' result is folded back with `dataclasses.replace(requirements, uninstalled=frozenset(...))`, never by assignment and never via `object.__setattr__`.\n- [ ] `Options.bad_imports`, `Options.uninstalled_imports` and `Options.extra_requirements` are deleted.\n- [ ] A test proves the replace is a rebind, not a mutation: the pre-repair `Requirements` still holds the pre-repair `uninstalled` set.\n\n**Verify:** `pixi run test tests/test_split_imports.py tests/test_classify.py -v` \u2192 green.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 5. Match `classify.split_imports`' actual annotations for `known_bad_imports` and `also_needs` rather than guessing \u2014 drop the conversions if it already accepts frozenset/Mapping. The `--reqs` block must build `extra_requirements` before classification and pass it in.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"src/veny/run_options.py\", \"tests/test_split_imports.py\", \"tests/test_classify.py\", \"tests/test_manifest_writing.py\"], \"verifyCommand\": \"pixi run test tests/test_split_imports.py tests/test_classify.py tests/test_manifest_writing.py -v && pixi run test\", \"acceptanceCriteria\": [\"pipeline.split_imports returns state.Requirements with no copy-back and no Options\", \"report takes (settings, scan, requirements)\", \"repairs are folded back with dataclasses.replace, never by assignment\", \"bad_imports, uninstalled_imports and extra_requirements are deleted from Options\", \"a test proves the pre-repair Requirements still holds its original uninstalled set\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        4
      ]
    },
    {
      "id": 6,
      "subject": "Task 6: VenvHandle replaces set_venv_dir",
      "status": "completed",
      "description": "**Goal:** The three venv-derived paths become one frozen value with a factory, `Options.set_venv_dir` and its five call sites go, and `install_succeeded` becomes a return value.\n\n**Files:**\n- Modify: `src/veny/state.py` (add `VenvHandle`)\n- Modify: `src/veny/pipeline.py:581-702` (`setup_virtualenv`), `:705-940` (`run`)\n- Modify: `src/veny/run_options.py` (delete `venv_dir`, `venv_python`, `requirements_file`, `install_succeeded`, `set_venv_dir`)\n- Modify: `tests/test_state_values.py`, `tests/test_venv_naming.py`, `tests/test_rename_venv.py`, `tests/test_uv_backend.py`\n\n**Acceptance Criteria:**\n- [ ] `state.VenvHandle` is frozen with exactly `venv_dir`, `venv_python`, `requirements_file`, and a `for_dir` classmethod that does the `mkdir(parents=True, exist_ok=True)` `set_venv_dir` did.\n- [ ] `venv_python` is `venv_dir / \"bin\" / \"python\"` and is **not** `.resolve()`d. A test pins it.\n- [ ] `Options.set_venv_dir` no longer exists; `rg -n 'set_venv_dir' src/ tests/` returns nothing. **This closes PROGRESS.md's \"`run_options.py` has never been through the STANDING CHECK\" item** \u2014 all five of its argument-carrying call sites were inside `set_venv_dir`.\n- [ ] `setup_virtualenv` returns `tuple[state.Requirements, state.VenvHandle | None, bool]`; `run` decides the rename from the returned flag.\n- [ ] The \"failed-\" rename still happens only when the script ran *and* the install succeeded, and still goes through `cache_search.rename_venv`.\n- [ ] The requirements file is still written **after** `environment.create_venv` succeeds, never before. A test pins the ordering.\n\n**Verify:** `pixi run test tests/test_state_values.py tests/test_uv_backend.py tests/test_venv_naming.py tests/test_rename_venv.py -v` \u2192 green; `rg -n 'set_venv_dir' src/ tests/` \u2192 no output.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 6. GOTCHA: `uv venv` refuses a non-empty directory and `for_dir` mkdirs it, so nothing may be written into it until `create_venv` has succeeded \u2014 writing earlier made every fresh build crash with CalledProcessError. Returning `None` for the handle covers both old failure paths (uv refused, or the final import check failed), which is what `setup_virtualenv` returning `False` used to mean.\n\n```json:metadata\n{\"files\": [\"src/veny/state.py\", \"src/veny/pipeline.py\", \"src/veny/run_options.py\", \"tests/test_state_values.py\", \"tests/test_venv_naming.py\", \"tests/test_rename_venv.py\", \"tests/test_uv_backend.py\"], \"verifyCommand\": \"pixi run test tests/test_state_values.py tests/test_uv_backend.py tests/test_venv_naming.py tests/test_rename_venv.py -v && rg -n 'set_venv_dir' src/ tests/ ; pixi run test\", \"acceptanceCriteria\": [\"state.VenvHandle is frozen with exactly three fields plus a for_dir classmethod that mkdirs\", \"venv_python is not resolved, and a test pins that\", \"set_venv_dir does not exist anywhere under src/ or tests/\", \"setup_virtualenv returns (requirements, handle_or_None, install_succeeded)\", \"a test pins that the requirements file is written only after create_venv succeeds\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        5
      ]
    },
    {
      "id": 7,
      "subject": "Task 7: Drain the last readers, repoint the suite",
      "status": "completed",
      "description": "**Goal:** Nothing in `pipeline.py` reads or writes `Options` except the two persistence calls, and every test that reached a drained field now builds the value instead.\n\n**Files:**\n- Modify: `src/veny/pipeline.py` (whole file \u2014 the final sweep)\n- Modify: `src/veny/run_options.py` (docstring: state exactly what is left and why)\n- Modify: every test file that still references a drained field\n\n**Acceptance Criteria:**\n- [ ] `rg -n 'options\\.' src/veny/pipeline.py` returns **only** lines inside `_load_last_used` and the `ek.save_options_to_json(options)` call.\n- [ ] `Options` retains exactly: `args`, `home`, `log_mode`, `rawlog`, `pathlibcutoff`, `options_json_filepath`, `stdlib`, `aliases`. Anything else is a leftover \u2014 delete it or write down why it survived.\n- [ ] `run_options.py`'s module docstring names 4b as the plan that deletes the file, and lists what is left.\n- [ ] `pixi run test` is green with **no** test asserting a drained `Options` attribute exists.\n- [ ] `tests/test_options_surface.py` tests the values, not the god object; rename it in the same commit if its name has stopped describing it.\n- [ ] The `cli.Options` / `veny.Options` reference count is **re-derived** and recorded in the commit message for 4b to use.\n\n**Verify:** `pixi run test` \u2192 green; `rg -c 'options\\.' src/veny/pipeline.py` \u2192 8 or fewer.\n\nFull steps with code: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 7. Re-measure the reference count, do not copy it: 3e predicted 42 and the real figure was 69; on `main` on 2026-08-21 it is 49 `cli.Options` (one is cli.py's own re-export) plus 28 `veny.Options` (one, `tests/test_json_types.py:174`, is a comment). Seven test files alias with `from veny import cli as veny`; find other aliases with `rg -n 'import cli as (\\w+)' tests/`. Do NOT add a conftest.py fixture for the value constructors \u2014 an explicit constructor call in the test body is the point of the phase.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"src/veny/run_options.py\", \"tests/\"], \"verifyCommand\": \"pixi run test && rg -n 'options\\\\.' src/veny/pipeline.py && pixi run lint && pixi run python -m ruff format --check . && pixi run typecheck\", \"acceptanceCriteria\": [\"options. appears in pipeline.py only inside _load_last_used and the save_options_to_json call\", \"Options retains exactly the eight named persistence/construction fields\", \"run_options.py's docstring names 4b as the plan that deletes it\", \"no test asserts a drained Options attribute exists\", \"the cli.Options / veny.Options reference count is re-derived and recorded in the commit message\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        6
      ]
    },
    {
      "id": 8,
      "subject": "Task 8: STANDING CHECK and the wiring index",
      "status": "completed",
      "description": "**Goal:** Every argument at every call site this plan changed is measured \u2014 is it read by the callee, and is there a test that fails if it is wrong \u2014 and the result is written down argument by argument.\n\n**Files:**\n- Create: `docs/superpowers/plans/2026-08-21-state-model-values-wiring-index.md`\n- Modify: whatever tests the sweep proves are missing\n\n**Acceptance Criteria:**\n- [ ] Every call site in `cli.py`, `pipeline.py` and `state.py` that this plan created or changed appears in the index, one row per argument.\n- [ ] Each row records: site, argument, whether the callee reads it, and the named test that fails if the value is wrong \u2014 or **OPEN HOLE** with a reason.\n- [ ] `state.VenvHandle.for_dir`'s call sites are in the index, and the preamble says explicitly that this closes the `run_options.py` STANDING CHECK gap 3e left.\n- [ ] Any argument measured DEAD is listed separately from the open holes, so the headline reads \"N holes **plus** M dead arguments\".\n- [ ] Every OPEN HOLE that a test *can* close, is closed in this task. Ones that cannot are recorded with the reason.\n\n**Verify:** `pixi run test` \u2192 green, with the new tests included; the index's row count matches `rg -c` over the changed call sites.\n\nFull steps: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 8. Build the row list from `git diff main`, NOT from memory \u2014 3e's Task 3 symbol sweep missed the whole `veny.Options` spelling because it worked from a list. For \"does a test fail if it is wrong\": mutate the value at the call site, run the suite, name the test that goes red, then revert. One mutation at a time, never batched. Each new test must state in its docstring the concrete bug that makes it fail (the `test-design` skill's standard).\n\n```json:metadata\n{\"files\": [\"docs/superpowers/plans/2026-08-21-state-model-values-wiring-index.md\", \"tests/\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"every changed call site appears in the index, one row per argument\", \"each row names the killing test or is marked OPEN HOLE with a reason\", \"VenvHandle.for_dir's sites are included and the preamble says this closes the run_options.py gap\", \"dead arguments are listed separately from open holes\", \"every closable OPEN HOLE is closed with a test in this task\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        7
      ]
    },
    {
      "id": 9,
      "subject": "Task 9: The phase 4a differential",
      "status": "completed",
      "description": "**Goal:** A script that drives the same inputs through `main()` in both trees and reduces the whole plan to a small, inspectable diff \u2014 and that is proved able to fail.\n\n**Files:**\n- Create: `scripts/differential_4a.py`\n\n**Acceptance Criteria:**\n- [ ] Modelled on `scripts/differential_3e.py` \u2014 read it first and match its layer structure and output format.\n- [ ] Drives at least: `--justprint` on a script with no third-party imports; `--justprint` on a script with one; `--blank-slate -y`; `--reqs`; a plain run that hits the cache; `--feeling-lucky` with no last-used record; and **a directory argument** (Task 1's sanctioned change \u2014 it must show up as a status-2 line, not a traceback).\n- [ ] Uses `git worktree add` for the baseline tree at `4d1846c`. **Never** `git checkout <sha>` inside the working tree.\n- [ ] Mutation-tested: four deliberate regressions, one per value object, each recorded as having grown the diff.\n- [ ] The residual-risk list \u2014 every input the differential does not drive \u2014 is in the script's docstring and goes into `PROGRESS.md` in Task 10.\n\n**Verify:** `pixi run python scripts/differential_4a.py` \u2192 prints the diff and its line count; the four mutation runs each produce a strictly larger diff than the clean run.\n\nFull steps: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 9. The four mutations: (1) drop the `dataclasses.replace` filling `python_command`; (2) build a second `Settings` inside `find_imports_in_script` with `rawlog=False` hardcoded; (3) skip the `dataclasses.replace` after `verify_and_repair_imports`; (4) `.resolve()` the interpreter path in `for_dir`. Revert each before the next. Clean up with `git worktree remove /tmp/veny-4a-baseline`. CLAUDE.md records a 2026-07-02 incident where `git checkout <sha>` inside the working tree silently restored deleted files \u2014 use the worktree.\n\n```json:metadata\n{\"files\": [\"scripts/differential_4a.py\"], \"verifyCommand\": \"pixi run python scripts/differential_4a.py\", \"acceptanceCriteria\": [\"the script matches differential_3e.py's layer structure and output format\", \"it drives all seven named layers including the directory argument\", \"the baseline tree comes from git worktree add at 4d1846c, never git checkout\", \"four mutations are each recorded as having grown the diff\", \"the residual-risk list is in the script docstring\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        7
      ]
    },
    {
      "id": 10,
      "subject": "Task 10: Close phase 4a",
      "status": "completed",
      "description": "**Goal:** Gates measured on the branch, `PROGRESS.md` updated with what this plan learned, and the next session pointed at 4b.\n\n**Files:**\n- Modify: `PROGRESS.md`\n- Modify: `docs/superpowers/specs/2026-08-15-veny-rearchitecture-design.md` (where an amendment contradicts it)\n- Modify: `README.md` (if `veny <directory>` was documented anywhere)\n\n**Acceptance Criteria:**\n- [ ] All four gates re-measured **in this session**, on this branch, with the numbers pasted from the terminal \u2014 not copied from a task report and not from `main`.\n- [ ] `PROGRESS.md`'s **Current work** block points at 4b as the next action, names this plan's merge commit, and lists what 4b inherits.\n- [ ] The five design amendments from the plan's preamble are recorded in `PROGRESS.md`, numbered on from 3e's last.\n- [ ] Retired items are struck from Deferred items, not duplicated: the folder-scanning ruling (decided and executed), the `run_options.py` STANDING CHECK gap (closed by Tasks 6 and 8), latent defect 2 (fixed by Task 1), the four `pipeline.py:125` dead-argument rows (closed by construction, Task 3).\n- [ ] What this plan did NOT do stays in Deferred items with its owner named: the in-virtualenv guard (4c), `--feeling-lucky`'s missing signal normalization (4c), latent defects 1 and 3 (4c), the remaining dead arguments (4c), the persistence change and the `Options` deletion (4b), design amendment 9 (4b), `pathlibcutoff`'s two readers (4b), the probe venv in classification (unowned), the single-file reachability gap (a later `analysis/` plan).\n- [ ] A live run on a real script, from a real shell, before the branch merges, with its output recorded and its install shape named.\n- [ ] `<plan>.md.tasks.json` agrees with git; where it does not, git wins and the tracker is fixed rather than the work redone.\n\n**Verify:** `pixi run test && pixi run lint && pixi run python -m ruff format --check . && pixi run typecheck` \u2192 paste all four outputs into the PROGRESS.md entry.\n\nFull steps: `docs/superpowers/plans/2026-08-21-state-model-values.md` \u00a7 Task 10. Baselines on `main` at `b59cfa8`: 408 passed, lint zero, format 55 files, typecheck 29 errors in 7 files. Task 6 deletes several `assert x is not None` lines that existed only for mypy narrowing, so the typecheck number may move either way \u2014 report it either way. Prove Task 1's change end to end with `pixi run veny some_directory/ ; echo \"exit status: $?\"` \u2192 status 2, no traceback. Run the whole-branch review BEFORE merging: 3b, 3c, 3d and 3e each had one and each found Important issues per-task review had missed. `pixi run` cannot reach the in-virtualenv branch (`sys.prefix == sys.base_prefix` there) \u2014 note the install shape used.\n\n```json:metadata\n{\"files\": [\"PROGRESS.md\", \"docs/superpowers/specs/2026-08-15-veny-rearchitecture-design.md\", \"README.md\"], \"verifyCommand\": \"pixi run test && pixi run lint && pixi run python -m ruff format --check . && pixi run typecheck\", \"acceptanceCriteria\": [\"all four gate outputs are pasted into PROGRESS.md, measured on this branch in this session\", \"PROGRESS.md's next action names 4b and lists what it inherits\", \"the five design amendments are recorded, numbered on from 3e's last\", \"closed items are moved out of Deferred items, not duplicated\", \"a live run is recorded, including `veny some_directory/` returning status 2 with no traceback\", \"the .tasks.json tracker agrees with git\"], \"modelTier\": \"standard\"}\n```",
      "blockedBy": [
        8,
        9
      ]
    }
  ],
  "lastUpdated": "2026-08-21T10:00:00Z"
}
