{
  "planPath": "docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Move Options into run_options.py",
      "status": "pending",
      "description": "**Goal:** `Options` lives in a module `pipeline.py` is allowed to import, with every existing `cli.Options` reference still working.\n\n**Files:**\n- Create: `src/veny/run_options.py`\n- Modify: `src/veny/cli.py:62-181` (delete the class, add the re-export)\n- Modify: `tests/test_layering.py` (add `run_options` to the `state` layer)\n- Test: `tests/test_layering.py`, `tests/test_options_surface.py`\n\n**Acceptance Criteria:**\n- [ ] `src/veny/run_options.py` holds the `Options` class, byte-identical in body to `cli.py:62-181` apart from its new module docstring and imports.\n- [ ] `cli.Options is run_options.Options` — a re-export, not a subclass or a copy.\n- [ ] `tests/test_layering.py` passes with `run_options` in the `state` layer and NO new `SANCTIONED_EXCEPTIONS` entry.\n- [ ] All 370 existing tests still pass, with no test file repointed.\n\n**Verify:** `pixi run test` → 371 passed; `pixi run typecheck` ≤ 33 errors\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 1.\n\n```json:metadata\n{\"files\": [\"src/veny/run_options.py\", \"src/veny/cli.py\", \"tests/test_layering.py\", \"tests/test_options_surface.py\"], \"verifyCommand\": \"pixi run test && pixi run lint && pixi run typecheck\", \"acceptanceCriteria\": [\"run_options.py holds the Options class verbatim\", \"cli.Options is run_options.Options\", \"test_layering passes with no new sanctioned exception\", \"371 tests pass\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Drive every branch of main() before it moves",
      "status": "pending",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Every branch `main()` can take is driven in process and asserted against an effect, so the move in Tasks 3–4 has something that can fail.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Modify: `tests/test_cli_entry_point.py` (extend `_drive_main` with a script-launch recorder, add six tests)\n- Test: `tests/test_cli_entry_point.py`\n\n**Acceptance Criteria:**\n- [ ] `main()`'s four post-classification branches are each driven in process: all-installed, in-virtualenv, cache-hit, cache-miss-then-build.\n- [ ] `--justprint` and `--blank-slate` are each driven in process and asserted on their effect (no script subprocess ran; the state directory was removed and `keep.json` survived), not on a spy's own argument.\n- [ ] Every new test names, in its docstring, the concrete bug that makes it fail.\n- [ ] Three named mutations are applied and each kills a named test: `sys.executable` swapped for `options.venv_python` in the all-installed branch; the justprint check returning instead of exiting; the blank-slate filter narrowed to `.out` only. Which test died is recorded in the commit message.\n\n**Verify:** `pixi run python -m pytest tests/test_cli_entry_point.py -v` → all pass, 6 added\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 2.\n\n```json:metadata\n{\"files\": [\"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cli_entry_point.py -v\", \"acceptanceCriteria\": [\"four post-classification branches driven in process\", \"--justprint and --blank-slate asserted on effect\", \"each new test names the bug that makes it fail\", \"three mutations each kill a named test, recorded in the commit message\"], \"modelTier\": \"frontier\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"task\", \"failurePolicy\": \"block\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Extract the analysis driver into pipeline.py",
      "status": "pending",
      "blockedBy": [
        1,
        2
      ],
      "description": "**Goal:** `pipeline.py` exists and owns everything from `list_packages` down; `cli.py` no longer knows how a script is scanned or classified.\n\n**Files:**\n- Create: `src/veny/pipeline.py`\n- Modify: `src/veny/cli.py` (delete `cli.py:699-955` and `cli.py:300-324`)\n- Modify: `tests/test_layering.py` (add the `pipeline` layer between `cache_search` and `cli`)\n- Modify: `tests/test_classify.py`, `tests/test_import_discovery.py`, `tests/test_cli_entry_point.py` (repoint moved symbols)\n- Test: the repointed files, plus `tests/test_layering.py`\n\n**Acceptance Criteria:**\n- [ ] `pipeline.py` holds `build_alias_index`, `find_imports_in_script`, `warn_about_system_packages`, `_probe_venv`, `split_imports`, `list_packages`, `stayed_out_dir` and `get_all_imports`, bodies unchanged apart from the `Options` annotation now reading `run_options.Options`.\n- [ ] `cli.py` contains none of those names, and no import left unused (established by `pixi run lint`, not by eye).\n- [ ] Test references repointed to `pipeline.*` — measured counts on `08622a8`: test_classify 25, test_import_discovery 4, test_layering 2, test_cli_entry_point 1. `scripts/differential_3d.py` is NOT updated (Task 9 explains why).\n- [ ] `tests/test_layering.py` passes with `pipeline` between `cache_search` and `cli`, no new sanctioned exception.\n\n**Verify:** `pixi run test` → 371 passed; `pixi run lint` clean; `pixi run typecheck` ≤ 33\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 3.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"src/veny/cli.py\", \"tests/test_layering.py\", \"tests/test_classify.py\", \"tests/test_import_discovery.py\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run test && pixi run lint && pixi run typecheck\", \"acceptanceCriteria\": [\"eight symbols moved verbatim to pipeline.py\", \"cli.py holds none of them and no unused imports\", \"test references repointed with measured counts\", \"layering guard passes with no new sanctioned exception\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Move the run itself into pipeline.py",
      "status": "pending",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** `main()` becomes argparse plus exit-status mapping; `pipeline.run` owns the sequence.\n\n**Files:**\n- Modify: `src/veny/pipeline.py` (gains `setup_virtualenv`, `_load_last_used`, `resolve_target`, `feeling_lucky`, `blank_slate`, `report`, `run_script`, `run`, `UsageError`, `VenvBuildFailed`)\n- Modify: `src/veny/cli.py:325-698` (delete `_load_last_used`, rewrite `main`)\n- Modify: `tests/test_cli_entry_point.py`, `tests/test_uv_backend.py`, `tests/test_verify.py`, `tests/test_last_used.py` (repoint stub targets)\n- Test: all of the above\n\n**Acceptance Criteria:**\n- [ ] `cli.main` is under 100 lines and contains no `logging.info` about the run, no `subprocess.run`, no cache search and no venv handling.\n- [ ] `pipeline.run(options) -> int` returns the status; the four script launches collapse to one `run_script`, with `announce=True` used ONLY at the acquired-venv launch.\n- [ ] `--justprint` and `--blank-slate` return 0 through `main()` rather than calling `sys.exit(0)` inside the run.\n- [ ] `feeling_lucky` still runs before `ek.configure_logging`, as today.\n- [ ] The negative-status normalization (`128 - status`) stays in `cli.main`.\n- [ ] Task 2's six tests pass with only their stub targets moved from `cli.*` to `pipeline.*`.\n\n**Verify:** `pixi run test` → 371 passed\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 4.\n\n```json:metadata\n{\"files\": [\"src/veny/pipeline.py\", \"src/veny/cli.py\", \"tests/test_cli_entry_point.py\", \"tests/test_uv_backend.py\", \"tests/test_verify.py\", \"tests/test_last_used.py\"], \"verifyCommand\": \"pixi run test && pixi run lint && pixi run typecheck\", \"acceptanceCriteria\": [\"cli.main under 100 lines, argparse and exit status only\", \"pipeline.run returns the status and owns the sequence\", \"one run_script with announce only at the acquired-venv launch\", \"feeling_lucky still precedes configure_logging\", \"Task 2 tests pass with only stub targets repointed\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: Delete --full and make a scriptless run exit 2",
      "status": "pending",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** Design ledger item 3 is closed — the flag, its help text, its README mention and all six of its branches are gone — and the fall-through it left behind is a clean exit 2.\n\n**Files:**\n- Modify: `src/veny/cli.py:220-224` (the `--full` argument)\n- Modify: `src/veny/pipeline.py` (branches originally at `cli.py:445-446`, `492-495`, `496-499`, `642`, `678-686`, `864-870`)\n- Modify: `README.md:77`\n- Test: `tests/test_cli_entry_point.py`\n\n**Acceptance Criteria:**\n- [ ] `rg -n -- '--full|\"full\"' src/ tests/ README.md` returns nothing.\n- [ ] `veny --full` exits 2 with argparse's own \"unrecognized arguments\" error — the flag is gone, not ignored.\n- [ ] `veny` with no script and no `--blank-slate` logs a message naming only flags that exist, and returns 2 via `pipeline.UsageError`.\n- [ ] `created_new_venv` and every other symbol that existed only to feed the deleted report are gone.\n- [ ] `get_all_imports` and the directory branch of `list_packages` STAY — a directory is still reachable as a positional argument.\n\n**Verify:** `pixi run python -m pytest tests/test_cli_entry_point.py -v` → the two new tests pass\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 5.\n\n```json:metadata\n{\"files\": [\"src/veny/cli.py\", \"src/veny/pipeline.py\", \"README.md\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cli_entry_point.py -v\", \"acceptanceCriteria\": [\"no --full anywhere in src, tests or README\", \"veny --full exits 2 from argparse\", \"no-script run returns 2 with a message naming only real flags\", \"created_new_venv deleted\", \"directory scanning still reachable positionally\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: Check the virtualenv veny is running inside",
      "status": "pending",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** Running veny from inside an active virtual environment checks that environment instead of raising `AssertionError`.\n\n**Files:**\n- Modify: `src/veny/last_used.py` (add `active_virtualenv_dir`, directly under `is_virtualenv`)\n- Modify: `src/veny/pipeline.py` (the `elif last_used.is_virtualenv():` branch, originally `cli.py:569-581`)\n- Test: `tests/test_last_used.py`, `tests/test_cli_entry_point.py`\n\n**Acceptance Criteria:**\n- [ ] `last_used.active_virtualenv_dir()` returns `$VIRTUAL_ENV` when set and `Path(sys.prefix)` otherwise.\n- [ ] The branch no longer contains `assert options.venv_dir is not None`.\n- [ ] A run inside a virtualenv that satisfies the imports runs the script and returns its status; one that does not returns 1 with today's two log lines unchanged.\n- [ ] Task 2's `test_main_reports_failure_when_the_surrounding_virtualenv_is_short_a_package` is replaced by `test_main_checks_the_virtualenv_it_is_running_inside`, which asserts the interpreter that was import-checked.\n\n**Verify:** `pixi run python -m pytest tests/test_last_used.py tests/test_cli_entry_point.py -v`\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 6.\n\n```json:metadata\n{\"files\": [\"src/veny/last_used.py\", \"src/veny/pipeline.py\", \"tests/test_last_used.py\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_last_used.py tests/test_cli_entry_point.py -v\", \"acceptanceCriteria\": [\"active_virtualenv_dir prefers VIRTUAL_ENV, falls back to sys.prefix\", \"no assert on options.venv_dir in the branch\", \"satisfying venv runs the script; short venv returns 1 with unchanged log lines\", \"the branch test asserts which interpreter was import-checked\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: Take exit ownership back into cli.py",
      "status": "pending",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** No module below `cli.py` raises `SystemExit` or lets a subprocess exception escape; design amendment 4 and design ledger item 4 are closed.\n\n**Files:**\n- Modify: `src/veny/environment.py` (add `UvUnavailable`; `uv_binary` raises it; `create_venv` returns `bool`; delete the redundant `# noqa: S603` at `environment.py:245`)\n- Modify: `src/veny/pipeline.py` (`setup_virtualenv` and `_probe_venv` consume the new return value)\n- Test: `tests/test_environment.py`, `tests/test_uv_backend.py`, `tests/test_cli_entry_point.py`\n\n**Acceptance Criteria:**\n- [ ] `rg -n 'SystemExit|sys\\.exit|my_critical_error' src/veny/ --glob '!cli.py'` returns nothing outside `__main__.py`'s `sys.exit(main())`.\n- [ ] `environment.create_venv` returns `bool` and its docstring no longer has a `Raises:` section.\n- [ ] A missing uv exits 1 with the current message text, on stderr.\n- [ ] A failed venv creation on the build path exits 1 with a logged message, not a `CalledProcessError` traceback; a failed probe venv raises `pipeline.VenvBuildFailed`, which `cli.main` maps to 1.\n- [ ] `environment.py:245`'s redundant `# noqa: S603` is gone and `pixi run lint` still reports `All checks passed!`. The load-bearing ones at `alias_index.py:523` and `stdlib_index.py:117` are untouched.\n\n**Verify:** `pixi run python -m pytest tests/test_environment.py tests/test_uv_backend.py -v && pixi run smoke` (if the network is unavailable, say so in the report rather than recording the task as verified)\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 7.\n\n```json:metadata\n{\"files\": [\"src/veny/environment.py\", \"src/veny/pipeline.py\", \"tests/test_environment.py\", \"tests/test_uv_backend.py\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_environment.py tests/test_uv_backend.py -v && pixi run smoke\", \"acceptanceCriteria\": [\"no SystemExit/sys.exit/my_critical_error below cli.py\", \"create_venv returns bool with no Raises section\", \"missing uv exits 1 with the current message on stderr\", \"failed venv creation exits 1 without a traceback\", \"redundant noqa at environment.py:245 removed and lint still clean\"], \"modelTier\": \"frontier\"}\n```"
    },
    {
      "id": 8,
      "subject": "Task 8: STANDING CHECK and the wiring index",
      "status": "pending",
      "blockedBy": [
        5,
        6,
        7
      ],
      "description": "**Goal:** Every argument at every call site this phase created kills a named test, measured mechanically — including the five `rawlog` holes 3d left open.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming-wiring-index.md`\n- Modify: whichever test files the holes require\n\n**Acceptance Criteria:**\n- [ ] The wiring index lists EVERY call site in `pipeline.py` and `cli.py` this phase created or moved, derived with `rg`, not copied from the plan's predicted 15-row table (3d predicted 14 and the sweep found 40).\n- [ ] For each argument: the substitution used, whether a named test died, and that test's name. Booleans appear TWICE, once per value — `True` alone left 16 of 17 `rawlog` sites green on 3d's branch.\n- [ ] Arguments with no natural empty value get a wrong-but-type-correct value, recorded so the evidence is auditable.\n- [ ] Zero open holes at the end, or each remaining one marked OPEN HOLE with the reason it cannot be closed.\n- [ ] The five `rawlog` sites 3d marked OPEN HOLE (`cli.py` 406, 503, 518, 1038, 1055) are closed by tests reading an EFFECT (a `caplog` record present or absent), never by a spy asserting the value it was handed. The `configure_logging` site is the one permitted spy and must be labelled as such in the index.\n- [ ] Every mutation is restored from a scratch copy — never `git stash`, never `git checkout -- <path>`.\n\n**Verify:** `pixi run test` → green after every hole is closed; the index's before/after hole counts are measured numbers, not estimates\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 8.\n\n```json:metadata\n{\"files\": [\"docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming-wiring-index.md\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"every call site derived mechanically with rg, not from the plan's table\", \"each argument's substitution and the named test that died recorded\", \"booleans substituted in both directions\", \"zero open holes or each marked OPEN HOLE with a reason\", \"the five inherited rawlog holes closed by effect-reading tests\"], \"modelTier\": \"frontier\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"task\", \"failurePolicy\": \"block\", \"requireEvidenceTokens\": [[\"before\", \"holes-before\"], [\"after\", \"holes-after\"]]}\n```"
    },
    {
      "id": 9,
      "subject": "Task 9: Write scripts/differential_3e.py",
      "status": "pending",
      "blockedBy": [
        5,
        6,
        7
      ],
      "description": "**Goal:** An old-vs-new comparison that drives `main()` itself — the one thing 3d's differential never did, and the blind spot that let 27 of its arguments go unpinned.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `scripts/differential_3e.py`\n- Modify: `scripts/differential_3d.py` (a historical-scope banner only — it reads `cli.list_packages`, `cli.setup_virtualenv` and `cli._load_last_used`, all moved by this phase, and is NOT updated)\n- Modify: `pyproject.toml` (per-file ignores, if the new script needs them)\n\n**Acceptance Criteria:**\n- [ ] The driver takes a tree root as an argument, sets `PYTHONHASHSEED=0` INSIDE itself, sets `sys.dont_write_bytecode`, purges `__pycache__` before the first import, redirects `HOME`, and prints `veny.cli.__file__` first. Reuse `differential_3d.py:83-152` verbatim for the scaffolding.\n- [ ] Diagnostics go to stderr; only compared state goes to stdout.\n- [ ] It drives `cli.main()` end to end for at least three argv shapes: nothing missing, a cache hit, and `--justprint`.\n- [ ] The cache-hit layer's two surviving candidates have DIFFERENT package counts, so `smallest` is distinguishable — 3d's could not distinguish it because both survivors tied.\n- [ ] The diff is empty apart from the deviations this plan sanctioned, each named in the script's docstring (layer 3 gains `logging.shutdown()`; a blank-slate layer would gain `ek.print_all_errors`).\n- [ ] The check is proved able to fail by at least four deliberate mutations, each recorded with the diff it produced: `sys.executable` swapped for the venv interpreter; `run_script` returning 0; `announce=True` dropped; cache ranking reversed.\n\n**Verify:** `git archive 08622a8 src/veny | tar -x -C /tmp/old-veny`, then run the driver against `/tmp/old-veny` and `/workspace` and `diff` the two captures\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 9.\n\n```json:metadata\n{\"files\": [\"scripts/differential_3e.py\", \"scripts/differential_3d.py\", \"pyproject.toml\"], \"verifyCommand\": \"git archive 08622a8 src/veny | tar -x -C /tmp/old-veny && pixi run python scripts/differential_3e.py /tmp/old-veny > /tmp/old.txt && pixi run python scripts/differential_3e.py /workspace > /tmp/new.txt && diff /tmp/old.txt /tmp/new.txt\", \"acceptanceCriteria\": [\"determinism scaffolding reused verbatim, diagnostics on stderr\", \"drives cli.main() for three argv shapes\", \"cache layer survivors have different package counts\", \"diff empty apart from the sanctioned deviations named in the docstring\", \"four deliberate mutations each produce a recorded diff\"], \"modelTier\": \"frontier\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"task\", \"failurePolicy\": \"block\", \"requireEvidenceTokens\": [[\"old\", \"old.txt\", \"08622a8\"], [\"new\", \"new.txt\", \"workspace\"]]}\n```"
    },
    {
      "id": 10,
      "subject": "Task 10: Close the phase with measured gates",
      "status": "pending",
      "blockedBy": [
        8,
        9
      ],
      "description": "**Goal:** The phase's evidence is recorded where the next session will find it, and nothing about it is inferred.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Modify: `README.md` (project structure: `cli.py`'s description is wrong after this phase; add `pipeline.py`, `run_options.py`, `differential_3e.py`)\n- Modify: `PROGRESS.md` (Current work, Deferred items, Gotchas)\n- Modify: `docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md` (annotate in place with `**[EXECUTION]**` blocks)\n\n**Acceptance Criteria:**\n- [ ] Every gate MEASURED and the number recorded: test count, lint, format file count, mypy errors WITH the per-file breakdown, smoke (and whether the network was available).\n- [ ] `wc -l` line counts recorded for every module.\n- [ ] Two live runs: `pixi run veny --no-cache` on a throwaway script importing a real third-party package builds a folder with no `failed-` prefix; the same script with NO flag logs `Using existing virtual environment:` naming THE SAME folder. Both folder names recorded verbatim.\n- [ ] `PROGRESS.md` records design amendments 12, 13 and 14, the six sanctioned deviations, what 3e declined and who owns it, and the residual risks this differential still cannot see.\n- [ ] The plan is annotated in place wherever its own text was wrong — 3b, 3c and 3d each found real errors this way.\n- [ ] Merged to `main` with `--no-ff`, and a whole-branch review requested before the branch is deleted.\n\n**Verify:** `pixi run test && pixi run lint && pixi run typecheck && pixi run smoke`, plus the two live runs above\n\nFull steps, code and rationale: docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md, Task 10.\n\n```json:metadata\n{\"files\": [\"README.md\", \"PROGRESS.md\", \"docs/superpowers/plans/2026-08-19-pipeline-and-cli-slimming.md\"], \"verifyCommand\": \"pixi run test && pixi run lint && pixi run typecheck && pixi run smoke\", \"acceptanceCriteria\": [\"every gate measured with numbers recorded, mypy per-file\", \"wc -l line counts recorded\", \"two live runs, second reusing the first's folder, both names recorded\", \"PROGRESS records amendments 12-14, the six deviations, declines and residual risks\", \"plan annotated in place with [EXECUTION] blocks\", \"merged --no-ff with a whole-branch review requested\"], \"modelTier\": \"frontier\", \"userGate\": true, \"tags\": [\"user-gate\"], \"requiresUserSpecification\": false, \"gateScope\": \"task\", \"failurePolicy\": \"block\", \"requireEvidenceTokens\": [[\"--no-cache\", \"built\", \"run 1\"], [\"Using existing virtual environment\", \"cache hit\", \"run 2\"]]}\n```"
    }
  ],
  "lastUpdated": "2026-08-19"
}
