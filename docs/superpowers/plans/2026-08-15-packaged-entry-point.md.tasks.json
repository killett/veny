{
  "planPath": "docs/superpowers/plans/2026-08-15-packaged-entry-point.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Move the modules into src/veny/",
      "status": "completed",
      "description": "**Goal:** The six root modules live in `src/veny/` as an importable package, with `__init__.py` and `__main__.py`, and the whole existing test suite passes unchanged in behaviour.\n\n**Files:**\n- Create: `src/veny/__init__.py`, `src/veny/__main__.py`\n- Move: `veny.py` \u2192 `src/veny/cli.py`; `alias_index.py`, `pypi_client.py`, `stdlib_index.py`, `venv_cache.py` \u2192 `src/veny/`; `veny_json_types.py` \u2192 `src/veny/json_types.py`\n- Modify: `src/veny/cli.py` (import lines only), `src/veny/alias_index.py:32`, `src/veny/json_types.py:21-22`\n- Modify: all 11 files under `tests/` that import veny's modules (including the subprocess source strings in `test_import_guard.py` and `test_json_types.py`)\n- Modify: `pyproject.toml` (pytest `pythonpath`), `.pre-commit-config.yaml`\n\n**Acceptance Criteria:**\n- [ ] No `.py` file for veny remains at the repository root\n- [ ] `pixi run test` reports 252 passed\n- [ ] `ruff check src/veny/cli.py --statistics` totals 299 or fewer findings\n- [ ] `mypy src/veny/cli.py src/veny/json_types.py` reports 28 or fewer errors\n- [ ] `git log --follow src/veny/cli.py` shows the pre-move history\n- [ ] The `ruff` and `ruff-format` pre-commit hooks skip `src/veny/cli.py`\n\n**Verify:** `pixi run test` \u2192 `252 passed`\n\nFull steps (14) with exact before/after code are in the plan document, Task 1.\n\n```json:metadata\n{\"files\": [\"src/veny/__init__.py\", \"src/veny/__main__.py\", \"src/veny/cli.py\", \"src/veny/alias_index.py\", \"src/veny/json_types.py\", \"pyproject.toml\", \".pre-commit-config.yaml\", \"tests/\"], \"verifyCommand\": \"pixi run test\", \"acceptanceCriteria\": [\"no veny .py file remains at the repository root\", \"pixi run test reports 252 passed\", \"ruff check src/veny/cli.py --statistics totals 299 or fewer\", \"mypy src/veny/cli.py src/veny/json_types.py reports 28 or fewer errors\", \"git log --follow src/veny/cli.py shows pre-move history\", \"ruff and ruff-format pre-commit hooks exclude src/veny/cli.py\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Delete the alias installer, pin identity",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** The alias installer, its four shell dialects and both flags are gone; `my_name` and argparse's `prog` are the constant `\"veny\"` rather than derived from `sys.argv[0]`.\n\n**Files:**\n- Create: `tests/test_cli_entry_point.py`\n- Modify: `src/veny/cli.py` (`Options.__init__`, `parse_arguments`, `main`, and the four alias functions)\n\n**Acceptance Criteria:**\n- [ ] `define_alias_command`, `alias_exists`, `add_alias_to_rc_file`, `add_alias`, `manual_instructions` and `my_filepath` no longer exist in `cli.py`\n- [ ] `--alias` and `--manual` both exit 2\n- [ ] `options.my_dir` is `~/veny` whatever `sys.argv[0]` says\n- [ ] `python -m veny --version` prints exactly `veny 0.2.2`\n- [ ] `pixi run test` reports 256 passed (252 + the 4 new cases)\n- [ ] `ruff check src/veny/cli.py --statistics` total is below 299\n\n**Verify:** `pixi run python -m pytest tests/test_cli_entry_point.py -v` \u2192 4 passed\n\nFull steps (10) with the complete test file and the exact blocks to delete are in the plan document, Task 2. TDD: write the 4 tests first and confirm all 4 fail before deleting anything.\n\n```json:metadata\n{\"files\": [\"tests/test_cli_entry_point.py\", \"src/veny/cli.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cli_entry_point.py -v\", \"acceptanceCriteria\": [\"define_alias_command, alias_exists, add_alias_to_rc_file, add_alias, manual_instructions and my_filepath are absent from cli.py\", \"--alias and --manual both exit 2\", \"options.my_dir is ~/veny regardless of sys.argv[0]\", \"python -m veny --version prints exactly 'veny 0.2.2'\", \"pixi run test reports 256 passed\", \"ruff check src/veny/cli.py --statistics total is below 299\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: Make the package installable",
      "status": "completed",
      "blockedBy": [
        1,
        2
      ],
      "description": "**Goal:** `pyproject.toml` declares a build backend, a `veny` console script and a single-sourced version, and `python -m build` produces a wheel whose entry point is `veny.cli:main`.\n\n**Files:**\n- Modify: `pyproject.toml`, `pixi.toml`, `.gitignore`\n\n**Acceptance Criteria:**\n- [ ] `pixi run build` writes `dist/veny-0.2.2-py3-none-any.whl` and a matching `.tar.gz`\n- [ ] The wheel's `entry_points.txt` contains `veny = veny.cli:main` under `[console_scripts]`\n- [ ] The wheel version comes from `src/veny/__init__.py`, with no `version` left in `[project]`\n- [ ] `pixi run veny --version` prints `veny 0.2.2`\n- [ ] `dist/`, `build/` and `*.egg-info/` are gitignored\n\n**Verify:** `pixi run build`, then read the wheel's `entry_points.txt` with the zipfile snippet in the plan \u2192 prints `[console_scripts]` and `veny = veny.cli:main`\n\nFull steps (7) with the exact TOML blocks are in the plan document, Task 3. Note the static `version = \"0.1.0\"` is deleted rather than updated \u2014 it contradicted `__version__ = \"0.2.2\"`.\n\n```json:metadata\n{\"files\": [\"pyproject.toml\", \"pixi.toml\", \".gitignore\"], \"verifyCommand\": \"pixi run build\", \"acceptanceCriteria\": [\"pixi run build writes dist/veny-0.2.2-py3-none-any.whl and a matching .tar.gz\", \"the wheel's entry_points.txt contains 'veny = veny.cli:main' under [console_scripts]\", \"the wheel version comes from src/veny/__init__.py with no version left in [project]\", \"pixi run veny --version prints 'veny 0.2.2'\", \"dist/, build/ and *.egg-info/ are gitignored\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": "4a",
      "subject": "Task 4a: Propagate the wrapped script's exit status",
      "status": "completed",
      "blockedBy": [
        3
      ],
      "description": "**Goal:** `veny script.py` exits with the status the script exited with, on every path that runs a script \u2014 so the Task 4 smoke gate can close as written.\n\n**Files:**\n- Modify: `src/veny/cli.py` (`main()`, three `subprocess.run` call sites, the final statement)\n- Modify: `src/veny/__main__.py`\n- Modify: `tests/test_cli_entry_point.py`\n\n**Acceptance Criteria:**\n- [x] The three script-running paths capture their `returncode`\n- [x] `main() -> int` returns that status after all existing cleanup\n- [x] `__main__.py` reads `sys.exit(main())`\n- [x] A script exiting 0 still makes `veny` exit 0\n- [x] `pixi run test` reports 257 passed\n\n**Verify:** `pixi run python -m pytest tests/test_cli_entry_point.py -v` \u2192 5 passed\n\nAdded mid-execution: Task 4's smoke check caught a real pre-existing defect and the human partner ruled it gets fixed in this branch. Complete at commits e4977cb..1770d3d.\n\n```json:metadata\n{\"files\": [\"src/veny/cli.py\", \"src/veny/__main__.py\", \"tests/test_cli_entry_point.py\"], \"verifyCommand\": \"pixi run python -m pytest tests/test_cli_entry_point.py -v\", \"acceptanceCriteria\": [\"the three script-running paths capture returncode\", \"main() -> int returns that status after all existing cleanup\", \"__main__.py reads sys.exit(main())\", \"a script exiting 0 still makes veny exit 0\", \"pixi run test reports 257 passed\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Prove the installed entry point works",
      "status": "completed",
      "blockedBy": [
        3,
        "4a"
      ],
      "description": "**Goal:** A `pixi run smoke` check that builds the wheel, installs it into a throwaway virtual environment, and proves both that the `veny` console script exists and that a wrapped script's exit status reaches the caller.\n\n**USER-ORDERED GATE \u2014 NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Create: `scripts/smoke-install.sh`\n\n**Acceptance Criteria:**\n- [ ] `pixi run smoke` exits 0 and its last line reads `smoke: OK`\n- [ ] The script fails loudly if `<venv>/bin/veny` is absent after installing the wheel\n- [ ] `veny --version` run from the throwaway venv prints `veny 0.2.2`\n- [ ] Running a fixture of `import sys; sys.exit(7)` through the installed `veny` exits **7**, not 0 and not 1\n- [ ] The run leaves no venv behind under the real `~/veny` (it runs with `HOME` pointed at the temporary directory)\n\n**Verify:** `pixi run smoke` \u2192 exit 0, final line `smoke: OK (console script installed, --version matched, exit status 7 propagated)`\n\nFull steps (6) including the complete script are in the plan document, Task 4. Step 4 is mandatory: temporarily change the fixture to `sys.exit(0)`, confirm the check FAILS with `smoke: FAIL \u2014 fixture exited 0, expected 7`, then restore it and confirm it passes. Capture both outputs \u2014 a check that cannot fail proves nothing.\n\n```json:metadata\n{\"files\": [\"scripts/smoke-install.sh\"], \"verifyCommand\": \"pixi run smoke\", \"acceptanceCriteria\": [\"pixi run smoke exits 0 with a final line starting 'smoke: OK'\", \"the script fails loudly when <venv>/bin/veny is absent\", \"veny --version from the throwaway venv prints 'veny 0.2.2'\", \"a fixture of 'import sys; sys.exit(7)' run through the installed veny exits 7\", \"the mutation check (sys.exit(0)) makes the smoke run FAIL, and restoring sys.exit(7) makes it pass\", \"no new venv folder appears under the real ~/veny\"], \"userGate\": true, \"tags\": [\"user-gate\"], \"gateScope\": \"task\", \"failurePolicy\": \"stop\", \"requireEvidenceTokens\": [[\"exit-7\", \"propagated\", \"smoke: OK\"], [\"exit-0\", \"mutation\", \"smoke: FAIL\"]], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: Update the documentation",
      "status": "completed",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** `README.md` documents the three install paths and how to remove the old alias; `PROGRESS.md` points at the new paths and drops the gotchas this work invalidated.\n\n**Files:**\n- Modify: `README.md`, `PROGRESS.md`\n\n**Acceptance Criteria:**\n- [ ] README's Installation section covers `uv tool install veny`, `uv tool install --editable <clone>`, and `pixi run veny`\n- [ ] README carries an \"Upgrading from the alias install\" section naming the exact line to delete and the `hash -r` / `rehash` follow-up\n- [ ] README's project-structure block shows `src/veny/` with the current filenames\n- [ ] PROGRESS.md's scoped-gate instructions name `src/veny/cli.py`, not `veny.py`\n- [ ] PROGRESS.md's \"flat script layout\" gotcha is replaced, not merely amended\n- [ ] PROGRESS.md's Current work block records Tasks 1-5 complete with Task 6 as the next action\n\n**Verify:** `rg -n 'uv tool install|Upgrading from the alias install|src/veny/cli\\.py' README.md PROGRESS.md` \u2192 hits in both files; `rg -n 'flat script layout' PROGRESS.md` \u2192 only inside the replacement's historical clause\n\nFull steps (7) with the complete README prose and the exact PROGRESS.md replacements are in the plan document, Task 5.\n\n```json:metadata\n{\"files\": [\"README.md\", \"PROGRESS.md\"], \"verifyCommand\": \"rg -n 'uv tool install|Upgrading from the alias install|src/veny/cli\\\\.py' README.md PROGRESS.md\", \"acceptanceCriteria\": [\"README Installation covers uv tool install veny, uv tool install --editable <clone>, and pixi run veny\", \"README has an 'Upgrading from the alias install' section with the exact line to delete and the hash -r / rehash follow-up\", \"README's project-structure block shows src/veny/ with current filenames\", \"PROGRESS.md scoped-gate instructions name src/veny/cli.py\", \"PROGRESS.md's flat-script-layout gotcha is replaced\", \"PROGRESS.md Current work records Tasks 1-5 complete and Task 6 next\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: Write the emmykit usage-audit prompt",
      "status": "pending",
      "blockedBy": [
        2,
        5
      ],
      "description": "**Goal:** A cross-repo prompt, in the same shape as the two existing ones, asking the emmykit repository whether its shell/alias helpers have any remaining callers \u2014 plus a pasteable `fd` command for searching everywhere else.\n\n**Files:**\n- Create: `docs/prompts/2026-08-15-emmykit-shell-alias-helpers-audit.md`\n- Modify: `PROGRESS.md` (Deferred items)\n\n**Acceptance Criteria:**\n- [ ] The prompt names all three functions (`detect_shell`, `find_shell_rc_file`, `find_additional_alias_files`) and all five `Options` fields (`shell`, `rc_file`, `alias`, `alias_command`, `additional_alias_files`), with their file paths\n- [ ] It asks for a usage audit and a recommendation, and explicitly does **not** ask for a deletion\n- [ ] It states that these are public API on a published 0.4.0, so removal is a breaking change\n- [ ] It carries an `fd` command, split across short lines, that counts occurrences under an arbitrary directory, plus a second variant for the attribute-access forms of the five fields\n- [ ] PROGRESS.md's Deferred items records the outstanding prompt\n\n**Verify:** run the prompt's own `fd` command against this repository \u2192 no output (before Task 2 the same command reported `./veny.py:3`)\n\nFull steps (5) with the complete prompt text and both `fd` commands are in the plan document, Task 6.\n\n```json:metadata\n{\"files\": [\"docs/prompts/2026-08-15-emmykit-shell-alias-helpers-audit.md\", \"PROGRESS.md\"], \"verifyCommand\": \"fd -t f -e py . . -E .pixi -E site-packages -E .git -X rg -c -w -e detect_shell -e find_shell_rc_file -e find_additional_alias_files\", \"acceptanceCriteria\": [\"the prompt names all three functions and all five Options fields with file paths\", \"it asks for an audit and a recommendation, not a deletion\", \"it states removal is a breaking change to a published 0.4.0\", \"it carries an fd command split across short lines, plus a variant for the attribute-access forms\", \"PROGRESS.md Deferred items records the outstanding prompt\", \"the fd command returns no hits when run against this repository\"], \"modelTier\": \"mechanical\"}\n```"
    }
  ],
  "lastUpdated": "2026-08-15"
}
